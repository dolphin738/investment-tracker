# 功能验收表（AI代码清理与瘦身 · 阶段 1 基线）

> 本文档为《AI代码清理与瘦身实施计划》阶段 1 产出物，作为后续所有代码删除操作的验收基线（「免死金牌」）。
> 全部条目基于**当前源码逐文件遍历**生成，未参考旧版验收表，未使用 docs/openapi.json（已确认严重过时）。

- **基线分支**：`cleanup/phase-0`
- **基线 tag**：`pre-cleanup`
- **生成日期**：2026-08-24
- **规模统计**：
  - 前端：**15 组 / 117 个功能点**（以 web/src/router/index.ts 注册的 13 个页面 + 1 重定向 + 404 为入口逐页记录）
  - 后端：**14 个模块（main.py 挂载 26 个 router 对象）/ 122 个 HTTP 接口**，另附 **8 条全局基础设施验收项**
- **优先级分布汇总**：

| 端 | P0 | P1 | P2 | 合计 |
|----|----|----|----|------|
| 前端 | 23 | 69 | 25 | 117 |
| 后端（接口） | 42 | 66 | 14 | 122 |
| 后端（基础设施） | 2 | 3 | 3 | 8 |
| 合计 | 67 | 138 | 42 | 247 |

> P0 = 出错即造成资损或数据丢失（认证、流水/交易/价格/余额/快照写入、组合删除、导入落库等）
> P1 = 用户高频主流程（查询展示、管理操作）；P2 = 低频或辅助功能

---

# 一、前端功能验收表

## FE-GLOBAL 全局布局与路由守卫

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-GLOBAL-01 | P0 | 认证守卫拦截未登录访问 | router.beforeEach（web/src/router/index.ts:123） | 未认证访问受保护路由 → replace 跳转 /login | 公开页 /login、/register 直接放行 | index.ts:127-143 |
| FE-GLOBAL-02 | P0 | 登录回跳意图记录 | sessionStorage AUTH_RETURN_KEY（router/index.ts:137） | 跳登录前把 to.fullPath（含 query/hash）写入 sessionStorage，登录成功后回跳深链 | 存储失败（隐私模式/配额）静默忽略；跳 /login 自身不覆盖已有意图 | index.ts:132-141 |
| FE-GLOBAL-03 | P2 | 顶部基准时钟 | AppLayout.vue:42-56 | 显示北京时间（UTC+8）YYYY-MM-DD HH:mm:ss，每秒刷新 | 仅重渲该 span | title 标注「项目基准日期时间」 |
| FE-GLOBAL-04 | P1 | 组合切换器 | PortfolioSelector + 新建组合入口（AppLayout.vue:112） | 切换 currentPortfolioId；「新建组合」打开 PortfolioDialog | — | AppLayout.vue:111-112,187-192 |
| FE-GLOBAL-05 | P2 | 用户菜单 | DropdownMenu（AppLayout.vue:114-161） | 头像/名称下拉 → 「设置」跳 /settings、「退出登录」清认证态并跳 /login | — | handleLogout AppLayout.vue:66-69 |
| FE-GLOBAL-06 | P2 | 移动端汉堡侧栏 | Menu 按钮（AppLayout.vue:77-84） | <md 折叠侧栏，遮罩点击关闭；Sidebar navigate 事件后收起 | — | AppLayout.vue:172-177 |
| FE-GLOBAL-07 | P1 | 服务端偏好引导加载 | PreferenceBootstrap 包裹 RouterView（AppLayout.vue:181） | 受保护路由首屏加载服务端偏好写入 preference.store，默认组合生效 | — | B4 批次注释 AppLayout.vue:9 |
| FE-GLOBAL-08 | P2 | 通知铃（仅 admin） | NotificationBell v-if isAdmin（AppLayout.vue:109） | 管理面站内信通知铃仅管理员可见 | — | ADR-002 §3 Q2 |
| FE-GLOBAL-09 | P2 | 404 兜底页 | /:pathMatch(.*)* → pages/not-found.vue（router/index.ts:110-114） | 任意未注册路径渲染 404 页 | — | — |
| FE-GLOBAL-10 | P1 | /transactions 重定向 | redirect { path: ROUTE_PATH.TRANSACTIONS }（router/index.ts:62-65） | /transactions → /cashflows（前端 301 语义） | — | FLOW-P0-01 验收4 |

## FE-LOGIN 登录页（/login）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-LOGIN-01 | P0 | 邮箱+密码登录 | LoginForm 表单提交 → POST /api/auth/login（LoginForm.vue:68-84） | 成功写 auth store 并回跳意图路由；按钮 pending 态禁用 + Loader | Zod 校验：邮箱格式、密码≥6 位；仅提交时校验 | loginSchema LoginForm.vue:39-42 |
| FE-LOGIN-02 | P0 | 注销冷静期恢复引导 | 业务码 1007 拦截（LoginForm.vue:73-83） | 登录返回 1007 时切换 AccountRestorePrompt 卡片，暂存已输入凭证一键恢复 POST /api/auth/account/restore | 其余错误码仍走 api-client 统一 toast；重新提交先清空子状态；remainingDays 缺省 ACCOUNT_RETENTION_DAYS(30) | LoginForm.vue:74-81 |
| FE-LOGIN-03 | P1 | 字段错误提示 | errors.email / errors.password 行内文案（LoginForm.vue:127-142） | 「请输入有效的邮箱」「密码至少 6 位」 | validateOnModelUpdate:false（不即时校验） | LoginForm.vue:62-63 |
| FE-LOGIN-04 | P2 | 跳转注册链接 | RouterLink → /register（LoginForm.vue:156-161） | — | — | — |

## FE-REGISTER 注册页（/register）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-REG-01 | P0 | 注册新账户 | RegisterForm 提交 → POST /api/auth/register（RegisterForm.vue:73-79） | 成功后跳登录页（use-register composable）；pending 禁用 | name 可选 ≤50 字；密码 ≥8 位且同时含字母和数字；两次密码一致 | registerSchema RegisterForm.vue:33-46 |
| FE-REG-02 | P2 | 密码强度实时检查项 | rules 计算属性（RegisterForm.vue:67-71） | 「至少 8 位 / 包含字母 / 包含数字」三项绿勾/灰叉实时切换 | — | — |
| FE-REG-03 | P2 | 返回登录链接 | RouterLink → /login（RegisterForm.vue:155-160） | — | — | — |

## FE-DASHBOARD 概览页（/ DashboardPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-OVW-01 | P1 | 无组合空态 | portfolios.length===0 分支（DashboardPage.vue:382-386） | 「欢迎，先创建您的第一个投资组合」引导 | — | — |
| FE-OVW-02 | P1 | 未选组合提示 | !currentPortfolioId 分支（DashboardPage.vue:389-393） | 「请先在顶部选择一个投资组合」 | — | — |
| FE-OVW-03 | P1 | 双查询失败重试态 | overviewIsError && latestNavError（DashboardPage.vue:408-421） | 「数据加载失败，请稍后重试」+「重新加载」按钮 refetch 两查询 | 单查询失败不触发该分支 | — |
| FE-OVW-04 | P1 | 关键指标 8 卡 | buildOverviewMetrics（DashboardPage.vue:323-349,469-495） | 资产构成 4（总资产/持仓市值/现金余额/净投入）+ 收益表现 4（累计收益率/当年收益率/年化XIRR/累计净值），分两组渲染 | 值与涨跌方向由 buildOverviewMetrics 统一构造含空值兜底；格式偏好（千分位/缩写/小数位）生效 | asset-metrics.ts |
| FE-OVW-05 | P1 | 数据截止日 + 新鲜度提示 | PageHeader description + FreshnessBanner + PriceFreshnessBadge（DashboardPage.vue:426-461） | 有 latestDate 显示「数据截止 X」；freshness 存在且 isStale 才渲染提示条；徽标常显 | — | DASH-P1-03 |
| FE-OVW-06 | P0 | 录入出入金弹窗入口 | 页头按钮 → CashflowForm Dialog（DashboardPage.vue:436-443,741-754） | 打开录入出入金弹窗，成功后关闭并失效相关缓存（CashflowForm 内部） | — | — |
| FE-OVW-07 | P0 | 录入买卖弹窗入口 | 页头按钮 → SecurityTradeForm Dialog（DashboardPage.vue:444-451,757-772） | 录入买卖流水并维护现价，成功关闭弹窗；持仓/净值自动推导 | — | — |
| FE-OVW-08 | P1 | 维度切换 + 区间筛选 | Tabs 日/周/月/年 + DateRangeQuickPicker（DashboardPage.vue:504-538） | g/range/from/to 写 URL（useUrlState），默认值不写 URL；range=custom 用 from/to；「全部」起点=组合首个交易日 | 用户交互后不再被偏好对齐弹回（useRangePreferenceSync markInteracted） | DashboardPage.vue:180-218 |
| FE-OVW-09 | P1 | 总资产走势 hero 图 | TotalAssetTrendChart（DashboardPage.vue:541-549） | 按 navSeries(metric=both) 渲染，含手工记录标记 | loading 骨架 | 缺陷4-B 注释 249-257 |
| FE-OVW-10 | P1 | 有组合无数据三步引导 | hasNoData 判定 + ONBOARDING_STEPS（DashboardPage.vue:357-362,552-591) | 三步卡：创建组合（已完成）/录入首笔存入/录入证券买卖；步骤按钮打开对应弹窗 | isError 不误判为空态（排除 isError） | DASH-P0-06 |
| FE-OVW-11 | P1 | 净值趋势双线图 | NavTrendChart（DashboardPage.vue:595-599） | 累计+当年双线（metric=BOTH 保证两字段下发） | 无数据提示「数据不足」 | — |
| FE-OVW-12 | P1 | XIRR 趋势图 | XirrTrendChart connectNulls=false（DashboardPage.vue:600-605） | null 断线不画 0 | — | — |
| FE-OVW-13 | P1 | 近期出入金最近 5 笔 | listTransactions(page=1,pageSize=5)（DashboardPage.vue:278-285,607-681） | 日期/类型（BUY=存入 up 色，SELL=取出 down 色）/+/-金额/备注截断 120px；「查看全部」跳 /cashflows | 空态 EmptyState + 录入按钮；loading TableSkeleton | — |
| FE-OVW-14 | P1 | 组合表现对比 | getPortfoliosSummary（DashboardPage.vue:288-293,683-736） | 每组合行显示名称/总资产/累计收益率/XIRR；红涨绿跌 | cumulativeReturnRate/xirr 用 !=null 判空防 NaN%；空列表「暂无组合数据」 | DashboardPage.vue:707-709 注释 |
| FE-OVW-15 | P2 | 概览 URL 参数持久化 | useUrlState createOverviewSchema（DashboardPage.vue:173-178） | 刷新/分享/前进后退还原 g/range/from/to | 默认值取偏好 defaultGranularity/defaultDateRange | AL-014/T03 |

## FE-HOLDINGS 持仓页（/holdings HoldingsPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-HOLD-01 | P1 | 页签持久化 | usePersistentTab('invest:holdings-tab')（HoldingsPage.vue:97） | 持仓/买卖明细/分红三 Tab，刷新后停留当前页签 | — | — |
| FE-HOLD-02 | P1 | I-05 统一筛选器 | HoldingsToolbar，状态单一来源=URL query（HoldingsPage.vue:102-131,326-332） | date/closed/types/sec/range/from/to/scenario 全走 useUrlState；非法值降级 | as-of 下限 minDate=baseDate→首笔交易→组合 createdAt→今天（缺陷3修复链路）HoldingsPage.vue:180-187 | — |
| FE-HOLD-03 | P1 | 偏好对齐一次性守卫 | rangeInteracted/closedInteracted watch（HoldingsPage.vue:116-162） | 偏好异步到达且用户未交互时对齐一次 closed=true 与默认 range；URL 已带参数或用户改过则不对齐 | — | QA 第1轮 Bug 修复注释 |
| FE-HOLD-04 | P1 | 持仓汇总 5 卡 | aggregate MetricCards（HoldingsPage.vue:345-369） | 总市值/总成本/浮盈(±)/总盈亏率/标的数，随筛选动态变化 | 浮盈正负号 + 红涨绿跌 | HOLD-B-P0-06 |
| FE-HOLD-05 | P1 | 持仓列表 11 列 | Table（HoldingsPage.vue:412-528） | 标的/代码/类型/数量/成本价/现价/成本额/市值/浮动盈亏/盈亏率/占比（数值+进度条），列顺序不可调 | 已清仓(qty=0) Badge「已清仓」；flag=COST_BASED Badge「成本估值」；正常持仓恒排已清仓之前，同组市值降序（前端排序 Q-5 甲） | sortedItems HoldingsPage.vue:259-266 |
| FE-HOLD-06 | P0 | 现价内联编辑 | InlinePriceEditor（HoldingsPage.vue:468-474） | 点击现价单元格内联修改 → 写 security-prices 并触发重算反馈 | priceAsOf 展示现价日期 | 对应 BE-PRC-02/03 |
| FE-HOLD-07 | P0 | 录入买卖弹窗 | 页头/空态按钮 → SecurityTradeForm（HoldingsPage.vue:314-321,399-408,554-564） | 打开买卖录入弹窗；空态文案区分「无标的」与「有标的无交易」两种引导 | INC-05：空态按钮与主入口同规格 | — |
| FE-HOLD-08 | P1 | 持仓类型筛选 | types 多选 → GET holdings types 参数（HoldingsPage.vue:206-212; calculation/router.py:138-150） | 类型多选传后端按 compute_type 过滤（override 优先，代码前缀推断兜底） | — | 修复问题4 注释 |
| FE-HOLD-09 | P1 | 买卖明细 Tab 联动筛选 | deriveTradeSecurityFilter（HoldingsPage.vue:222-250,532-540） | 类型/证券多选派生 securityId 集合；scenario BUY/SELL → side 参数；日期范围透传 | loading/empty 短路不发查询（组件内 filterState 三态） | trade-security-filter.ts |
| FE-HOLD-10 | P2 | 分红 Tab | DividendList 接收 sec/dateRange（HoldingsPage.vue:543-550） | 分红独立记录，不参与收益计算 | scenario 不再传导分红板块 | HOLD-B-P0-10 / INC-04 |
| FE-HOLD-11 | P1 | 持仓加载失败重试态 | ErrorState + refetchHoldings（HoldingsPage.vue:372-382,273-276） | 「数据加载失败」+ 重新加载按钮 | — | — |
| FE-HOLD-12 | P2 | 无结果空态 | EmptyState（HoldingsPage.vue:387-409） | 暂无持仓数据 + 引导按钮 | — | — |
| FE-HOLD-13 | P2 | 新鲜度徽标 | PriceFreshnessBadge（HoldingsPage.vue:313） | 基于 sync-status 的轻量指示 | — | price-freshness.ts |

## FE-CASHFLOW 出入金管理页（/cashflows TransactionsPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-CF-01 | P1 | 统一筛选器（双页签共享） | 类型复选 + DateRangeQuickPicker + 排序 Select + 重置（TransactionsPage.vue:314-389） | 日期范围对两页签同时生效；类型/排序仅作用于流水；变更即写 URL query 生效无需确认按钮 | HelpTip 说明作用范围；重置=清空全部 query 回「全部+date desc+第1页20条」 | TransactionsPage.vue:188-190 |
| FE-CF-02 | P1 | 快捷范围受控回显 | urlRange/quickValue computed（TransactionsPage.vue:136-151） | URL range 为唯一真相源；手动改日期回显「自定义」；全空回落偏好 defaultRange | 不在挂载时反写 URL | INC-01 |
| FE-CF-03 | P1 | 出入金流水列表 | CashflowList + URL 分页参数（TransactionsPage.vue:398-420） | 按 startDate/endDate/types/sortBy/sortOrder/page/pageSize 展示；排序仅非默认时透传后端 | 编辑/删除触发后端重算（列表描述文案） | F5 白名单注释 220-229 |
| FE-CF-04 | P0 | 录入出入金 | 页头按钮 → CashflowForm Dialog（TransactionsPage.vue:302-309,479-493） | 提交 POST cashflows 成功关弹窗并触发缓存失效 | — | — |
| FE-CF-05 | P1 | 流水编辑/删除 | CashflowList 行操作 → PATCH/DELETE cashflows（CashflowList.vue） | 触发区间重算，返回 recalculation 反馈 | — | 后端见 BE-CF-04/05 |
| FE-CF-06 | P0 | 现金余额录入/编辑 | CashBalanceForm Dialog 新增/编辑复用（TransactionsPage.vue:234-244,496-518） | 新增 editingBalance=null，编辑传入行数据；保存/删除均触发净值/XIRR 重算 | 同一弹窗组件复用 | — |
| FE-CF-07 | P1 | 当前余额展示 | useLatestCashBalance（TransactionsPage.vue:433-449） | 显示当前余额 + 「自 X 日起沿用」；未维护时提示「请点击右上角录入现金余额」 | — | CASH-P0-02 验收1 |
| FE-CF-08 | P2 | 余额维护两条提示 | Info 列表（TransactionsPage.vue:452-461） | 存取与买卖不会自动调整此值；修改后自动总资产重算但跳过手工日期 | — | CASH-P0-03 |
| FE-CF-09 | P1 | 余额变更历史 | CashBalanceHistory 受统一日期范围约束（TransactionsPage.vue:464-473） | 每条可编辑/删除 | — | — |
| FE-CF-10 | P1 | 软提示引导更新余额 | CASH_BALANCE_FOCUS_EVENT 监听（TransactionsPage.vue:246-259） | 出入金/买卖录入后的软提示「去更新」→ 程序化切到现金余额页签并打开录入弹窗 | 只引导不自动修改 CashBalance | FLOW-P0-06 |
| FE-CF-11 | P2 | 流水页签持久化 | usePersistentTab('invest:transactions-tab')（TransactionsPage.vue:105） | 刷新停留当前页签 | 页签标识不写 URL | — |

## FE-SNAPSHOT 资产记录页（/snapshots SnapshotsPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-SNAP-01 | P1 | 历史记录列表 | SnapshotList（SnapshotsPage.vue:139-144） | 日期/总资产/持仓/现金/来源(自动/手工)/系统自动值+差异%/备注/操作 | 默认日期范围=偏好（I-04），pageSize 20 | SnapshotsPage.vue:69-81 |
| FE-SNAP-02 | P0 | 录入资产记录 | SnapshotForm Dialog（SnapshotsPage.vue:120-128,163-176） | 日期不可未来、总资产必填；保存后成为手工记录并触发重算 | 系统自动值覆盖提示 | snapshot-schema.ts |
| FE-SNAP-03 | P0 | 编辑（变手工） | 行编辑 → SnapshotForm 带 snapshot（SnapshotsPage.vue:86-88,179-198） | 自动行保存后变手工记录 | v-if 卸载重建使表单重置 | — |
| FE-SNAP-04 | P0 | 删除记录 | SnapshotList 行删除 | 删除该日记录，事件日会重新生成自动值 | — | — |
| FE-SNAP-05 | P0 | 重置（撤销手工） | 行重置 → POST snapshots/{date}/reset | 恢复系统计算值，仅手工记录可用 | — | 底部图例 SnapshotsPage.vue:148-160 |
| FE-SNAP-06 | P1 | 导出 CSV 快捷入口 | 导出 CSV 按钮 → export type=assetSnapshots format=csv（SnapshotsPage.vue:56-65,106-114） | 下载当前组合全部资产记录 CSV；pending 显示「导出中…」禁用 | — | SET-P0-03 |
| FE-SNAP-07 | P2 | 底部图例 HelpTip | （SnapshotsPage.vue:148-160） | 解释沿用/按成本/每天唯一/编辑/删除/撤销语义 | — | §7.3 |

## FE-XIRR 收益分析页（/analysis/xirr XirrAnalysisPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-XIRR-01 | P1 | 当前累计 XIRR 卡 | useLatestXirr（XirrAnalysisPage.vue:211-222） | hero 卡显示当前 XIRR + 最新日期；无数据显示「暂无数据」 | 小数位=偏好 xirrDecimals | — |
| FE-XIRR-02 | P1 | 较年初变化 | useYearStartXirr 独立查询（XirrAnalysisPage.vue:116-126,223-230） | 当年首个非空 XIRR 与当前值差（单位 pp），up/down trend | 基准与页面维度/范围解耦（Part A2 修复） | ANL-P0-04 |
| FE-XIRR-03 | P1 | 维度+范围切换 | DimensionSwitcher 受控（XirrAnalysisPage.vue:201-209） | 日/周/月/年 × 快捷范围；quick 字段剥离后才发请求（避免后端 forbidNonWhitelisted 400） | 偏好对齐守卫防弹回 | toDimensionQueryParams :113 |
| FE-XIRR-04 | P1 | XIRR 趋势折线图 | XirrTrendChart connectNulls=false（XirrAnalysisPage.vue:232-238） | null 断线；标题随粒度变化（按日/周/月/年） | — | — |
| FE-XIRR-05 | P2 | 年度柱状图 | YearlyBarChart highlight-current-year（XirrAnalysisPage.vue:240-246） | 按年聚合取每年最后一条非空值；当年柱高亮 | 仅非「按年」维度且有数据时渲染 | aggregateByYear :165-178 |
| FE-XIRR-06 | P1 | 明细表（倒序+环比） | detailRows computed（XirrAnalysisPage.vue:135-142,248-288） | 日期/XIRR/环比变化三列，最新在前 | 空 series 显示「暂无数据」；loading Skeleton | — |

## FE-NAV 净值分析页（/analysis/nav NavAnalysisPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-NAV-01 | P1 | 指标单选（累计/当年/对比） | RadioGroup METRIC_OPTIONS（NavAnalysisPage.vue:54-59,90） | metric 传后端；对比模式双线 | — | — |
| FE-NAV-02 | P1 | 四指标卡 | 当前累计净值/当年净值/累计收益/当年收益 | 数值随 latestNav 与序列更新；小数位/金额格式偏好生效 | — | NavAnalysisPage.vue:66-74 |
| FE-NAV-03 | P1 | 净值趋势双线图 | NavTrendChart 按 metric 筛选 | 对比模式累计+当年双线 | — | — |
| FE-NAV-04 | P2 | 月度收益热力图 | MonthlyHeatmap 正红负绿色阶 | — | — | — |
| FE-NAV-05 | P1 | 每日净值明细表 | computeDailyDetails（NavAnalysisPage.vue:52; nav-daily-details.ts） | 日期/累计净值/当年净值/每日收益/收益%/份额；每日收益=(当日累计净值-前日)×前日份额 | 正红负绿 | — |
| FE-NAV-06 | P1 | 维度+聚合偏好 | DimensionSwitcher aggregation 读偏好（NavAnalysisPage.vue:85-86） | ANL-P0-03：默认聚合读偏好 | 偏好对齐守卫 | — |

## FE-ACCOUNT 账户页（/account AccountPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-ACC-01 | P2 | 个人信息卡（只读） | AccountInfoCard（AccountPage.vue:33） | 展示资料；「前往设置修改 →」跳转 | 零修改控件 | ACC-P0-02 |
| FE-ACC-02 | P1 | 资产全景卡（只读） | AssetOverviewCard（AccountPage.vue:34） | 聚合展示账户资产 | — | ACC-P0-03 |
| FE-ACC-03 | P2 | 数据统计卡（只读） | StatsOverviewCard（AccountPage.vue:35） | 组合数/总资产/累计XIRR/当年XIRR 等 | — | ACC-P0-06 |
| FE-ACC-04 | P0 | 我的组合·新建/编辑 | PortfolioDialog 双模式（PortfolioManagementCard.vue:78-88） | 新建/编辑组合名称与描述 → POST/PATCH /portfolios | — | ACC-P0-04 |
| FE-ACC-05 | P0 | 我的组合·删除 | AlertDialog 二次确认 → DELETE /portfolios/{id}（PortfolioManagementCard.vue:80-86） | 删除组合及其全部子数据 | 二次确认弹窗 | — |
| FE-ACC-06 | P1 | 我的组合·归档 toggle | Archive 按钮 → PATCH /portfolios/{id}/archive（PortfolioManagementCard.vue:87） | 归档/取消归档；已归档组合星标 disabled | — | — |
| FE-ACC-07 | P1 | 设为默认星标 toggle | Star → PATCH /portfolios/{id}/default（PortfolioManagementCard.vue:43-44,96-101） | 点击设默认/再点取消；点击后立即高亮 | 已归档 disabled | — |
| FE-ACC-08 | P1 | 统一表格（业绩+管理列合并） | summary + portfolios 按 id merge（PortfolioManagementCard.vue:3-12,90-94） | 业绩列来自 GET /portfolios/summary，管理列来自 GET /portfolios | SYS-P0-05：null 渲「—」绝不伪造 0；元信息缺失降级合成不抛错 | — |
| FE-ACC-09 | P2 | 点击组合名切换并跳概览 | 行名点击 → setCurrent + push '/'（PortfolioManagementCard.vue:9） | — | — | — |
| FE-ACC-10 | P1 | 行情自动同步配置 | AutoSyncCard：开关+周期+时间+weekday/day_of_month → PUT /api/quote-sync（AutoSyncCard.vue:5-10） | 保存后后端 reload_schedule；非启用也允许保存 | WEEK 选星期几、MONTH 选几号 | — |
| FE-ACC-11 | P1 | 立即同步 | 「立即同步」→ POST /api/quote-sync/trigger（AutoSyncCard.vue:9） | 手动触发本人组合同步 | — | — |
| FE-ACC-12 | P2 | 上次执行结果徽标 | STATUS_VARIANT/LABEL（AutoSyncCard.vue:65-77） | RUNNING/SUCCESS/FAILED 徽标 + 时间 + 消息 | — | — |

## FE-SETTINGS 设置页（/settings SettingsPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-SET-01 | P2 | 账户信息摘要 | 头像/昵称/邮箱/手机号脱敏/简介（SettingsPage.vue:171-176,286-357） | 手机号 3****7 脱敏；「前往账户中心 →」 | — | — |
| FE-SET-02 | P1 | 修改邮箱 | ChangeEmailDialog → PATCH /api/auth/email（SettingsPage.vue:763-765） | 需当前密码校验；成功返回新 token | — | — |
| FE-SET-03 | P0 | 修改密码 | ChangePasswordDialog → PATCH /api/auth/password（SettingsPage.vue:767-769） | 需当前密码；成功返回新 token | — | — |
| FE-SET-04 | P1 | 编辑资料 | EditProfileDialog → PATCH /api/auth/profile + 头像上传并列（SettingsPage.vue:771-773,354-356） | 名称/头像/电话/简介；头像在本地上传与 URL 并列 | — | §7.8 L1318 |
| FE-SET-05 | P1 | 退出登录 | 按钮 → authStore.logout + push /login（SettingsPage.vue:347-350） | — | — | — |
| FE-SET-06 | P1 | 偏好表单（服务端持久化） | 13 项偏好字段 prefForm（SettingsPage.vue:121-135,360-653） | 默认组合/默认维度/默认日期范围/聚合方式/周起始日/净值小数位/XIRR小数位/主题/快照过期阈值/软提示×2/千分位/万亿缩写 | 服务端到达后回填；hasPrefChanges 控制保存可用；无变更显示「已是最新」；失败红字提示 | — |
| FE-SET-07 | P1 | 保存偏好乐观更新 | useUpdatePreferences（SettingsPage.vue:248-265） | 乐观更新失败回滚；指定新默认组合时切当前视图，「不设置」不动视图 | staleDays 输入钳制 1~30（onStaleInput） | — |
| FE-SET-08 | P2 | 货币/语言占位 | muted 文本 + HelpTip（SettingsPage.vue:610-629） | 「人民币 CNY 暂不可改」「中文简体暂不可改」，无 disabled 控件 | — | 待后端集成 |
| FE-SET-09 | P1 | 导出面板 | ExportPanel 7 类多选+格式串行下载（SettingsPage.vue:666-677） | type 多选 csv/xlsx；未选组合提示 | — | SET-P0-03 |
| FE-SET-10 | P0 | 导入预览→提交 | ImportDialog + ImportTemplateButtons（SettingsPage.vue:679-706） | 选类型→选文件(.csv/.xlsx/.xls)→预览(前10行+全量错误,不落库)→确认提交；成功 toast「新增 N 行，更新 M 行；已重算 X 起 N 天」 | 导入前提示先导出备份；切换类型重置文件 | FLOW-P1-01 / SET-P0-04；ImportDialog.vue:5-9 |
| FE-SET-11 | P0 | 清空当前组合数据 | AlertDialog 手输组合名确认 → DELETE /portfolios/{id}/data（SettingsPage.vue:714-737,831-883） | 列出将删除的 6 类数据；名称精确匹配才可确认；保留组合本身 | 未选组合禁用按钮 | SET-P0-05 |
| FE-SET-12 | P0 | 注销账户 | AlertDialog 手输邮箱确认 → DELETE /api/auth/account（SettingsPage.vue:739-757,776-829） | 软删除保留 30 天自助恢复文案；严禁「联系客服」表述 | 邮箱精确匹配才可确认 | SET-P1-06 / PRD §7.8 L1400 |

## FE-ADMIN-FIN 金融数据接口页（/admin AdminPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-ADM-01 | P1 | 非管理员整页无权限 | useIsAdmin 门控（AdminPage.vue:70-74） | 「无权限访问该页面」 | — | — |
| FE-ADM-02 | P2 | 子模块页签持久化 | usePersistentTab('invest:admin-active-module')（AdminPage.vue:59） | 三模块：接口API来源/接口分类管理/股票列表和测试 | — | — |
| FE-ADM-03 | P1 | 数据来源管理 | QuoteProviderSection CRUD（AdminPage.vue:92） | 提供方新增（HTTPS 必填 base_url / SDK 必填 sdk_name）/编辑/删除(级联删接口)/启停 enabled | 顶层「新增数据来源」按钮经 ref 调 openCreate | AdminPage.vue:61-62,85-88 |
| FE-ADM-04 | P1 | 接口分类管理 | InterfaceCategorySection | 分类 CRUD（label 必填）；系统内置分类隐藏删除入口 | — | admin/router.py:223 注释 |
| FE-ADM-05 | P1 | 接口 CRUD + 拖拽调序 | ProviderInterfaces / reorder.ts | 新增接口（category 不存在→400 拦截）、编辑（asset_class 多选/resp_* 解析字段/response_parse）、删除、同分类拖拽 reorder → priority=index | — | admin/router.py:322-327,502-515 |
| FE-ADM-06 | P1 | 股票列表与测试 | StockListPanel / StockListTestSection | 主数据目录分页浏览（q 匹配 code/name/拼音首字母；asset_class/exchange 过滤；类别+交易所稳定排序）、按类别统计、批量/单行删孤儿主数据、单接口测试（raw+parsed 回传）、全量同步触发 | masters 列表/stats 任意登录用户可读；delete/sync/test 仅 admin | admin/router.py:658-725,728-799,802-824 |
| FE-ADM-07 | P2 | 管理面全量行情刷新 | POST /quote-providers/sync 入口 | 遍历全部组合同步行情并重建快照/净值，返回结构化汇总 | — | admin/router.py:396-425 |

## FE-ADMIN-TASK 定时任务页（/admin/tasks SchedulePage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-TASK-01 | P1 | 任务列表（普通/系统分页） | useTasks + listTab 持久化（SchedulePage.vue:89-96） | NORMAL 前 SYSTEM 后两类页签；含最近一次执行摘要 | 非管理员无权限提示（composable 内控制发起） | — |
| FE-TASK-02 | P1 | 新建普通任务 | Dialog 表单（taskType/name/cron/desc/maxLogs/enabled/params）（SchedulePage.vue:108-220） | 类型清单来自 handlers.creatable；cron 经 CronInput + describeCron 中文描述；参数按 param_fields 渲染 | 系统任务不在可建列表 | schedule.py:232-261 |
| FE-TASK-03 | P1 | 编辑任务（系统任务受限） | 编辑 Dialog editTab basic/rules（SchedulePage.vue:122-123） | 系统任务可改 cron/启停/参数/名称/描述，类型与归类只读；普通任务全可改 | LOG_CLEANUP 含分级保留参数（retention_days/max_rows map_of JSON 编辑） | hasLeveledParams SchedulePage.vue:154-157 |
| FE-TASK-04 | P0 | 删除任务（仅普通） | AlertDialog → DELETE /tasks/{id} | 系统任务不可删（后端 400） | — | schedule.py:309-324 |
| FE-TASK-05 | P1 | 立即执行 | Play → POST /tasks/{id}/trigger | 写入 MANUAL 来源执行日志 | — | schedule.py:327-338 |
| FE-TASK-06 | P1 | 快速启停 | Switch → PATCH enabled | — | — | — |
| FE-TASK-07 | P1 | 执行日志抽屉 | useTaskLogs Dialog 分页 | 按开始时间倒序分页展示 status/trigger_source/message/error | — | schedule.py:341-358 |

## FE-ADMIN-LOGS 日志中心（/admin/logs LogCenterPage.vue）

| 编号 | 优先级 | 功能点 | 交互元素/接口 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| FE-LOG-01 | P1 | 角色门控 | useHasRole('admin','auditor')（LogCenterPage.vue:78） | 非 admin/auditor 直达深链被拦（菜单已先行过滤） | — | — |
| FE-LOG-02 | P1 | 聚合日志列表 | GET /api/admin/logs（LogCenterPage.vue:6-10） | 时间范围+级别+作用域+模块+关键字筛选；级别/来源/作用域色标徽标；未读通知圆点；分页 20/页 | 三源 UNION ALL 统一排序分页（绝不分源各查一页再拼） | log_center.py:4-7 |
| FE-LOG-03 | P1 | 详情弹窗 | useLogDetail GET /{log_id} | 前缀解析 app:/notif:/job: 后返回完整 LogItem（堆栈/附加信息可展开） | — | log_center.py:218-293 |
| FE-LOG-04 | P0 | 批量/单行删除（仅 admin） | useDeleteLogs DELETE /api/admin/logs | 当前页全选与跨页全选（all=true 按当前筛选跨页）；未读通知由后端跳过计入 skipped | auditor 不可删 | log_center.py:306-436 |

---

# 二、后端接口验收表

> 信封契约：全部业务 router 使用 EnvelopeRoute 自动包信封 `{code, data, message}`；业务异常 BusinessException → 统一信封错误码；HTTP 异常映射 HTTP_STATUS_TO_CODE（backend/app/core/enums.py:44-51）。组合归属隔离：get_portfolio 非本人或不存在一律 404 不泄露存在性（app/common.py:20-31）。分页统一返回 `{items,total,page,pageSize}`（common.py:34-52）。

## BE-HEALTH health（契约冒烟，7 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-HLTH-01 | P2 | 健康检查 | GET /api/health | `{status:"ok", service:"investment_return_tracker"}` | — | health/router.py:37-39 |
| BE-HLTH-02 | P2 | data=null 归一 | GET /api/empty | data 归一为 null | — | :42-45 |
| BE-HLTH-03 | P2 | Decimal 序列化 | GET /api/echo | 裸 Decimal 在信封中序列化为字符串非 number | — | :48-51 |
| BE-HLTH-04 | P2 | DecimalStr DTO | GET /api/decimal-model | DTO DecimalStr 字段输出字符串 | — | :54-57 |
| BE-HLTH-05 | P1 | JWT 保护样例 | GET /api/protected | 有效 Bearer 返回 user_id/email；无效 401/1001 | — | :60-62 |
| BE-HLTH-06 | P2 | 业务异常信封 | GET /api/boom | 3001/404「组合不存在」信封 | — | :65-72 |
| BE-HLTH-07 | P2 | demo token 签发 | GET /api/token | 幂等创建 demo-user-id 用户并签发 token | ⚠️ 公开无鉴权，任何人都可获得 demo token 访问受保护资源——Phase 0 冒烟遗留 [待人工确认] | :75-92 |

## BE-AUTH auth（9 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-AUTH-01 | P0 | 注册 | POST /api/auth/register | 创建用户返回 UserPublicOut | 邮箱重复 → EMAIL_TAKEN 1003/409 | auth/router.py:27-38; user.py:45 |
| BE-AUTH-02 | P0 | 登录 | POST /api/auth/login | 校验凭证返回 accessToken + user | 凭证错误（含密码错/邮箱不存在，统一口径防枚举）→ UNAUTHORIZED 1001/401「邮箱或密码错误」；账户注销冷静期 → PENDING_DELETION 1007/409 + data.remainingDays | :41-56; user.py:61-74（先验密再判定，注释「避免枚举」） |
| BE-AUTH-03 | P0 | 账户恢复 | POST /api/auth/account/restore | 邮箱+密码恢复软删除账户并签发 token | 未注销 → ACCOUNT_NOT_DELETED 1008/409；超期 → RESTORE_EXPIRED 1009/410；密码错同登录口径 UNAUTHORIZED 1001/401（防枚举） | :59-74; user.py:76-96 |
| BE-AUTH-04 | P1 | 当前用户 | GET /api/auth/me | JWT 解析 + DB 校验（存在且未软删）返回 UserPublicOut（含 createdAt，2026-08-25 缺陷修复后） | token 过期 1002/403；用户已删 → NOT_FOUND | :77-92 |
| BE-AUTH-05 | P1 | 完整资料读取 | GET /api/auth/profile | 返回含 createdAt 的完整资料（Web 客户端绑定路径） | — | :94-114 |
| BE-AUTH-06 | P1 | 更新资料 | PATCH /api/auth/profile | 部分更新 name/avatar/phone/bio | — | :117-134 |
| BE-AUTH-07 | P0 | 修改密码 | PATCH /api/auth/password | 校验 currentPassword 后更新并签发新 token | 当前密码错 → 1004/400 | :137-157 |
| BE-AUTH-08 | P0 | 修改邮箱 | PATCH /api/auth/email | 校验当前密码后换绑并签发新 token | 新邮箱已被占用 → 1003/409 | :160-180 |
| BE-AUTH-09 | P0 | 注销账户（软删除） | DELETE /api/auth/account | 软删除账户，冷静期内可恢复 | — | :183-189 |

## BE-AGGREGATION aggregation（6 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-AGG-01 | P1 | 全部组合摘要行 | GET /api/portfolios/summary | PortfolioSummaryRow[]（dashboard 对比 + 账户列表绑定） | 字面路由必须先于 /{portfolio_id} 注册 | aggregation/router.py:39-49 |
| BE-AGG-02 | P1 | 多组合对比 | GET /api/portfolios/comparison | PortfolioSummaryOut[]（与 summary 形状不同） | 同上 | :52-57 |
| BE-AGG-03 | P1 | 单组合关键指标 | GET /api/portfolios/{id}/summary | PortfolioSummaryOut | — | :60-65 |
| BE-AGG-04 | P1 | 概览核心指标+趋势 | GET /api/portfolios/{id}/overview?range= | range ∈ 1w/1m/3m/6m/1y/ytd/all 缺省 1y；含 freshness/holdingsSummary/xirr/nav/netInvested/latestDate/latestSource | 非法 range 静默回退为不限区间（=all，宽松容错设计，对齐上游 app/，裁决不改码）——aggregation.py:529-542 | :68-74 |
| BE-AGG-05 | P2 | 最大回撤序列 | GET /api/portfolios/{id}/metrics/drawdown | startDate/endDate 可选过滤 DrawdownPointOut[] | — | :77-84 |
| BE-AGG-06 | P1 | 账户统计 | GET /api/account/stats | 组合数/总资产/累计XIRR/当年XIRR（AccountStatsOut） | — | :87-92 |

## BE-PORTFOLIO portfolio（11 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-PF-01 | P1 | 组合列表 | GET /api/portfolios | 当前用户全部组合（createdAt desc） | — | portfolio/router.py:27-33 |
| BE-PF-02 | P0 | 新建组合 | POST /api/portfolios | PortfolioCreateReq → PortfolioOut | — | :36-43 |
| BE-PF-03 | P1 | 组合详情 | GET /api/portfolios/{id} | 归属隔离 404 | — | :46-48 |
| BE-PF-04 | P1 | 编辑组合 | PATCH /api/portfolios/{id} | PortfolioPatchReq 部分更新 | — | :51-58 |
| BE-PF-05 | P0 | 删除组合 | DELETE /api/portfolios/{id} | 删除组合及全部子数据（级联） | ⚠️ 不可逆 | :61-67 |
| BE-PF-06 | P0 | 清空组合数据 | DELETE /api/portfolios/{id}/data | 保留组合本身，级联删子表，返回 deletedCount | — | :70-77 |
| BE-PF-07 | P0 | 行情同步（同步等待） | POST /api/portfolios/{id}/prices/sync | 完整 fallback 链 Σtimeout 封顶，commit 落库，返回 {synced,failed,skipped,errors} | 写 security_prices + 重建快照/净值 | :83-94 |
| BE-PF-08 | P0 | 行情异步刷新 | POST /api/portfolios/{id}/prices/refresh-async | fire-and-forget 独立会话后台跑完整链，立即 202 {accepted,portfolio_id} | 进程重启丢任务；异常 rollback 吞掉 | :97-116 |
| BE-PF-09 | P1 | 同步状态轮询 | GET /api/portfolios/{id}/prices/sync-status | 返回最新 fetched_at + source | 无价格记录两字段 null | :119-136 |
| BE-PF-10 | P1 | 归档 toggle | PATCH /api/portfolios/{id}/archive | archived true/缺省→archivedAt=now，false→置空；归档时若为默认组合同步置空偏好 | — | :139-151 |
| BE-PF-11 | P1 | 设默认 toggle | PATCH /api/portfolios/{id}/default | 五角星 toggle，返回 PreferenceOut | — | :154-162 |

> 注：portfolio 模块共 11 个接口（含 prices 三路端点与 archive/default 两个 toggle）。

## BE-DATA data（六组 29 接口）

### BE-CF 出入金 cashflows（5 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-CF-01 | P1 | 流水分页列表 | GET /api/portfolios/{pid}/cashflows | startDate/endDate/types(BUY,SELL 白名单外忽略)/page/pageSize | 非法 types 片段忽略 | data/router.py:68-85 |
| BE-CF-02 | P1 | 流水详情 | GET .../cashflows/{cf_id} | CashflowOut；不存在 3001/404 | — | :88-93 |
| BE-CF-03 | P0 | 录入流水 | POST .../cashflows | 创建 + RecalculationService 区间重算；响应附 recalculation{fromDate,affectedDays,skippedManualDays} | — | :96-108 |
| BE-CF-04 | P0 | 编辑流水 | PATCH .../cashflows/{cf_id} | 同上重算反馈 | — | :111-125 |
| BE-CF-05 | P0 | 删除流水 | DELETE .../cashflows/{cf_id} | 删除 + 重算；返回 recalculation 反馈对象 | — | :128-140 |

### BE-SEC 标的 securities（5 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-SEC-01 | P1 | 标的分页列表 | GET .../securities | Paginated[SecurityOut] | — | data/router.py:151-159 |
| BE-SEC-02 | P1 | 标的详情 | GET .../securities/{sec_id} | SecurityOut；不存在 404 | — | :162-167 |
| BE-SEC-02b | P0 | 懒实例化 resolve | POST .../securities/resolve | 幂等 upsert by (portfolio_id, master_id)；必须选中目录主数据；响应附 isNew | master 不存在 → 业务异常 | ADR-003 :170-183 |
| BE-SEC-03 | P1 | 编辑标的 | PATCH .../securities/{sec_id} | SecurityPatchReq（override 类型等） | — | :186-194 |
| BE-SEC-04 | P0 | 删除标的 | DELETE .../securities/{sec_id} | FK ondelete=CASCADE 级联删交易/价格；有成交日则重算并反馈 recalculation，无成交日返回 null | — | :197-211 |

### BE-TRADE 证券买卖 trades（5 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-TRD-01 | P1 | 买卖分页列表 | GET .../security-trades | securityId/side/startDate/endDate/page/pageSize 过滤 | — | data/router.py:222-236 |
| BE-TRD-02 | P1 | 买卖详情 | GET .../security-trades/{trade_id} | TradeOut；不存在 404 | — | :239-244 |
| BE-TRD-03 | P0 | 录入买卖 | POST .../security-trades | TradeCreateReq 创建并触发级联重算 | §9.2 卖出硬校验（service 层） | :247-252; trade.py:106,136,200 |
| BE-TRD-04 | P0 | 编辑买卖 | PATCH .../security-trades/{trade_id} | 更新 + 重算 | 同上卖出校验 | :255-263 |
| BE-TRD-05 | P0 | 删除买卖 | DELETE .../security-trades/{trade_id} | 删除 + 重算，返回 None | — | :266-271 |

### BE-PRICE 最新价 prices（4 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-PRC-01 | P1 | 价格分页列表 | GET .../security-prices | securityId 过滤 | — | data/router.py:282-291 |
| BE-PRC-02 | P0 | 录入价格 | POST .../security-prices | PriceCreateReq 创建（内联现价编辑落点） | — | :294-299 |
| BE-PRC-03 | P0 | 编辑价格 | PATCH .../security-prices/{price_id} | 更新 + 重算 | — | :302-310 |
| BE-PRC-04 | P0 | 删除价格 | DELETE .../security-prices/{price_id} | 删除 + 重算 | — | :313-318 |

### BE-CB 现金余额 cash-balances（4 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-CB-01 | P1 | 余额分页列表 | GET .../cash-balances | asOf 过滤 | — | data/router.py:329-338 |
| BE-CB-02 | P0 | 录入余额 | POST .../cash-balances | 创建（生效日起前向沿用）+ 重算 | — | :341-346 |
| BE-CB-03 | P0 | 编辑余额 | PATCH .../cash-balances/{cb_id} | 更新 + 重算（手工日期跳过） | — | :349-357 |
| BE-CB-04 | P0 | 删除余额 | DELETE .../cash-balances/{cb_id} | 删除 + 重算 | — | :360-365 |

### BE-SNAP 总资产快照 snapshots（6 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-SNP-01 | P1 | 快照分页列表 | GET .../snapshots | startDate/endDate/source(MANUAL/AUTO) 过滤；每项附 derivedTotalAsset | — | data/router.py:376-394 |
| BE-SNP-02 | P1 | 按日期查快照 | GET .../snapshots/{snap_date} | SnapshotOut + derivedTotalAsset | — | :397-402 |
| BE-SNP-03 | P0 | 录入手工快照 | POST .../snapshots | 每日唯一；手工取代当天自动；触发重算 | 日期冲突/未来日期校验（snapshot.py:97 业务异常） | :405-410 |
| BE-SNP-04 | P0 | 编辑快照（变手工） | PATCH .../snapshots/{snap_id} | 更新后来源变 MANUAL | — | :413-421 |
| BE-SNP-05 | P0 | 删除快照 | DELETE .../snapshots/{snap_id} | 删除该日记录；事件日会重新生成自动值 | — | :424-429 |
| BE-SNP-06 | P0 | 重置快照 | POST .../snapshots/{snap_date}/reset | 撤销手工修改恢复系统计算值 | 仅手工记录可用 | :432-437 |

## BE-DIVIDEND dividend（4 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-DIV-01 | P1 | 分红分页列表 | GET .../dividends | securityId/startDate/endDate 过滤，pageSize 默认 50；JOIN 标的信息 | — | dividend/router.py:33-63 |
| BE-DIV-02 | P0 | 录入分红 | POST .../dividends | 创建；netAmount=amount-tax 由后端计算不落库；不写 CashFlow、不触发重算引擎 | securityId 必须属于同一组合（防跨组合挂载，dividend.py:73） | :66-76 |
| BE-DIV-03 | P1 | 编辑分红 | PATCH .../dividends/{div_id} | 更新 | 同上二级隔离校验（dividend.py:121） | :79-90 |
| BE-DIV-04 | P1 | 删除分红 | DELETE .../dividends/{div_id} | 删除 | — | :93-98 |

## BE-DT data_transfer（4 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-DTX-01 | P1 | 导出 | GET /api/portfolios/{pid}/export?type=&format= | 7 类 ExportType；csv/xlsx 文件直出绕过信封（Content-Disposition 附件名含组合名+日期） | format 非法 → VALIDATION_FAILED 2000/400 | data_transfer/router.py:56-78,37-45 |
| BE-DTX-02 | P0 | 导入预览 | POST .../import/preview (multipart: type,file) | 校验扩展名(.csv/.xlsx/.xls)、大小≤5MB；validate_and_build 返回 totalRows/validRows/sample/errors/minDate/token（不落库） | 超限/格式错 → 2000/400 | :81-125 |
| BE-DTX-03 | P0 | 导入提交 | POST .../import/commit | 解码 token 校验 purpose/type/portfolio_id 匹配；单事务落库 + 单次重算 | token 类型/组合不匹配 → 2000/400；token 过期（data_transfer.py:293,299,454） | :128-150 |
| BE-DTX-04 | P1 | 下载导入模板 | GET /api/data-transfer/template?type=&format= | 3 类 ImportType；模板列=导入列，首行示例；文件直出 | 需登录；format 非法 400 | :156-187 |

## BE-PREF preference + quote_sync（5 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-PREF-01 | P1 | 读取偏好 | GET /api/users/preferences | 不存在则建默认返回（get_or_create） | — | preference/router.py:25-31 |
| BE-PREF-02 | P1 | 更新偏好 | PATCH /api/users/preferences | body 为 dict，服务端白名单校验部分更新 | 非白名单键 → 业务异常（preference.py:39,83,104） | :34-41 |
| BE-QS-01 | P1 | 读取同步配置 | GET /api/quote-sync | 无配置返回默认结构不落库 | — | quote_sync.py:84-92 |
| BE-QS-02 | P1 | 保存同步配置 | PUT /api/quote-sync | 校验 time(HH:MM)/frequency(DAY/WEEK/MONTH)/weekday(1-7)/day_of_month(1-31)，upsert 后 reload_schedule | 非法 → 400 各字段对应消息 | :95-114,43-52 |
| BE-QS-03 | P1 | 立即同步触发 | POST /api/quote-sync/trigger | run_user_sync_now 当前用户本人组合，立即返回 triggered | — | :117-122 |

## BE-UPLOAD upload（1 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-UPL-01 | P1 | 头像上传 | POST /api/upload/avatar (multipart file) | jpeg/png/webp 白名单+魔数嗅探双重校验；≤2MB；落盘 uuid.ext；更新 user.avatar；旧文件 best-effort 删；返回手工信封 {code:0,data:{url,user}} | 类型/大小/内容不符/缺失 → FILE_INVALID 1006/400；未登录 401/1001 | upload/router.py:26-46; upload.py:48,56,66 |

## BE-CALC calculation（9 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-CALC-01 | P1 | 持仓推导（只读） | GET /api/portfolios/{pid}/holdings | asOf(缺省今天)/securityId(逗号分隔多个也支持)/includeClosed/types 过滤；items 附 securityCode/Name/Type/quantity/avgCost/marketPrice/priceAsOf/pnl/pnlRate/flag(COST_BASED/EXACT)；aggregate 五指标 | cost_total=0 时 ratio=0 | calculation/router.py:108-202 |
| BE-CALC-02 | P1 | XIRR 序列 | GET .../xirr?granularity&startDate&endDate&aggregation | day/week/month/year 分桶 last/avg；DailyXirr 时间序列 | — | :211-230 |
| BE-CALC-03 | P1 | 最新 XIRR | GET .../xirr/latest | 最近一条非空 xirr_value；无则 None | — | :233-245 |
| BE-CALC-04 | P2 | XIRR 历史（分页） | GET .../xirr/history | month/last 缺省，内存分页 Paginated | — | :458-473 |
| BE-CALC-05 | P1 | NAV 序列 | GET .../nav?metric=cumulative/year/both | 同时返回 value+cumulativeNav+yearNav+shares（未选中置 null，缺陷5/4-B）；分桶聚合 | metric both 时 value=null | :254-304 |
| BE-CALC-06 | P1 | 最新 NAV | GET .../nav/latest | 最近一条 DailyNav；无则 None | — | :312-329 |
| BE-CALC-07 | P2 | NAV 历史（分页） | GET .../nav/history | 内存分页 Paginated[NavPointOut] | — | :440-455 |
| BE-CALC-08 | P1 | 区间重算 | POST .../recalculate-range | startDate 缺省取四表最早事件日；无事件返回 {affectedDates:0,duration:0}；返回耗时 ms | — | :338-351 |
| BE-CALC-09 | P1 | 全量重算 | POST .../recalculate | 从最早事件日重算至今 | 同上 | :354-362 |

## BE-INTERNAL internal（2 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-INT-01 | P0 | 账户物理清理 | POST /api/internal/cleanup/accounts | 外部 cron 触发（建议每日 04:00）；物理清除过冷静期的账户 | X-Internal-Token 校验，缺失/不符 → 403；幂等 | internal/router.py:33-41 |
| BE-INT-02 | P2 | 头像孤儿清理 | POST /api/internal/cleanup/avatars | 清理孤儿头像文件（建议每 3 个月） | 同上 token 保护 | :44-52 |

## BE-ADMIN admin 主路由（24 接口，全部 require_admin 除注明）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-ADM-01 | P1 | 提供方列表 | GET /api/admin/quote-providers | 全部提供方 | — | admin/router.py:239-246 |
| BE-ADM-02 | P0 | 新增提供方 | POST 同路径 | access_method=https 必填 base_url / sdk 必填 sdk_name（model_validator）；ValueError → 400 | — | :249-268,51-58 |
| BE-ADM-03 | P1 | 提供方详情 | GET /{provider_id} | 不存在 404 | — | :287-297 |
| BE-ADM-04 | P1 | 编辑提供方 | PATCH /{provider_id} | 局部更新；access_method+config 同传才校验 | — | :354-378 |
| BE-ADM-05 | P0 | 删除提供方 | DELETE /{provider_id} | 级联删除其下接口，返回 {id,deleted:true} | — | :381-393 |
| BE-ADM-06 | P1 | 全部接口扁平总览 | GET /quote-providers/interfaces | 顶层按分类汇总；必须注册在 /{provider_id} 之前 | — | :271-284 |
| BE-ADM-07 | P1 | 提供方接口列表 | GET /quote-providers/{pid}/interfaces | 该提供方全部接口 | — | :300-308 |
| BE-ADM-08 | P0 | 新增接口 | POST 同路径 | provider 不存在 → 404；category_id 不存在 → 400 预校验（防 IntegrityError 兜底成 500） | — | :311-351 |
| BE-ADM-09 | P1 | 接口详情 | GET /quote-providers/interfaces/{iid} | 404 兜底 | — | :431-441 |
| BE-ADM-10 | P1 | 编辑接口 | PATCH 同路径 | category_id 显式非 None 时预校验；None=置未分类合法 | — | :444-484 |
| BE-ADM-11 | P0 | 删除接口 | DELETE 同路径 | {id,deleted:true} | — | :487-499 |
| BE-ADM-12 | P1 | 接口拖拽调序 | PATCH /quote-interfaces/reorder | ordered_ids → priority=index；跨分类混入/不存在 id → 400 | — | :502-515 |
| BE-ADM-13 | P0 | 管理面全量行情刷新 | POST /quote-providers/sync | 遍历全部组合同步行情+重建快照/净值，返回 {portfolios,synced,failed,errors} | 长耗时同步等待 | :396-425 |
| BE-ADM-14 | P2 | 通知列表 | GET /notifications | created_at 倒序；前端据 read 算未读数 | — | :521-528 |
| BE-ADM-15 | P1 | 通知标已读 | POST /notifications/{nid}/read | 不存在 → 404 | — | :531-540 |
| BE-ADM-16 | P1 | 分类列表 | GET /interface-categories | sort_order 升序；含 system 标记（固定 2 内置类） | — | :546-553,223 |
| BE-ADM-17 | P1 | 新增分类 | POST 同路径 | label 必填；系统分类同名冲突在 service 层校验 | — | :556-569 |
| BE-ADM-18 | P1 | 编辑分类 | PATCH /{cid} | 404 兜底 | — | :572-591 |
| BE-ADM-19 | P1 | 删除分类 | DELETE /{cid} | 不影响接口；系统分类不可删（service 层） | — | :594-607 |
| BE-ADM-20 | P1 | 主数据列表 | GET /securities/masters | **任意登录用户可读**（录入搜索复用）；q ILIKE code/name/拼音首字母；asset_class/exchange 过滤；类别 STOCK 置顶/未分类垫底 + 交易所 SH<SZ<BJ<HK 稳定排序 | page≥1, pageSize≤200 | :658-702（get_current_user :671） |
| BE-ADM-21 | P2 | 主数据统计 | GET /securities/masters/stats | 按资产类别计数；NULL 归 UNCATEGORIZED | 任意登录用户可读 | :705-725 |
| BE-ADM-22 | P0 | 批量删孤儿主数据 | DELETE /securities/masters | ids 或 all=true（按筛选跨页定位）；被组合持仓引用/不存在计入 skipped，仅删孤儿；单事务一次 commit | ids 空 → 400；非 admin 403 | :728-799 |
| BE-ADM-23 | P0 | 主数据全量同步 | POST /securities/sync | 配置驱动遍历全部 MASTER_LIST 接口资产类别 | — | :802-810 |
| BE-ADM-24 | P1 | 单接口测试 | POST /quote-interfaces/{iid}/test | 用传入 params 调用原样回传 raw+parsed，不计入 consecutive_failures | — | :813-824 |

## BE-SCHEDULE admin/schedule（7 接口，除 logs 外全部 require_admin）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-SCH-01 | P2 | 可建任务类型清单 | GET /api/admin/tasks/handlers | 返回 5 种任务类型元数据（label/creatable/param_fields），供前端表单渲染；SECURITY_MASTER_SYNC、LOCAL_COMMAND、HTTP_CALLBACK 可建，ACCOUNT_CLEANUP、LOG_CLEANUP 为系统任务不可新建 | — | admin/schedule.py:38-94,176-189 |
| BE-SCH-02 | P1 | 任务列表 | GET /api/admin/tasks | 全部任务（含最近一次执行摘要 last_run_*，按创建时间升序） | — | :209-229 |
| BE-SCH-03 | P0 | 新建普通任务 | POST /api/admin/tasks | 校验 cron 可被 APScheduler 解析（非法 → 400）、任务名唯一（重复 → 400）；系统任务类型 → 400；创建后 reload_schedule | max_logs ≤0 视为不限制 | :232-261 |
| BE-SCH-04 | P1 | 编辑任务 | PATCH /api/admin/tasks/{id} | 普通任务可改全部字段；系统任务改 task_type → 400「系统任务类型不可修改」；改名重名校验；每次保存后 reload_schedule | 不存在 404 | :264-306 |
| BE-SCH-05 | P0 | 删除任务 | DELETE /api/admin/tasks/{id} | 仅普通任务可删；系统任务 → 400；删除后 reload_schedule | 不存在 404 | :309-324 |
| BE-SCH-06 | P1 | 手动立即执行 | POST /api/admin/tasks/{id}/trigger | run_task_now 写入 MANUAL 来源执行日志，立即返回 triggered | 不存在 404 | :327-338 |
| BE-SCH-07 | P2 | 执行日志分页查询 | GET /api/admin/tasks/{id}/logs | 按 started_at 倒序分页 | ⚠️ 权限为 get_current_user（任意登录用户可读），与同组其余 require_admin 端点不一致 [待人工确认] | :341-358 |

## BE-LOGCENTER admin/log_center（3 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-LGC-01 | P1 | 三源聚合日志列表 | GET /api/admin/logs | app_logs+notifications+job_run_logs 单条 UNION ALL CTE 归一，统一列上过滤（level/scope/module/start/end/keyword ILIKE）+ created_at 倒序 + 分页；id 加前缀 app:/notif:/job: | level ∈ error/warning/info、scope ∈ operation/error/system（pattern 校验）；admin/auditor 可读 | log_center.py:155-215,9 |
| BE-LGC-02 | P1 | 日志详情 | GET /api/admin/logs/{log_id} | 解析 `<source>:<uuid>` 前缀查对应源返回完整 LogItem（含 trace）；无冒号按 app 处理 | 非法来源/不存在 → 404 | :218-293 |
| BE-LGC-03 | P0 | 批量删除日志 | DELETE /api/admin/logs | ids 或 all=true（复用筛选跨页定位）；按来源分桶删除；未读通知一律跳过计入 skipped（硬规则）；单事务一次 commit | 仅 admin（auditor 403）；ids 空 → 400；来源非法/不存在 skipped | :306-436 |

## BE-CLOG client_log（1 接口）

| 编号 | 优先级 | 功能点 | 方法+路径 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-CLOG-01 | P2 | 客户端错误上报 | POST /api/client-logs | 前端全局错误捕获/unhandledrejection/api-client 失败分支上报写入 app_logs（scope='client'）并附 user_id | 任意登录角色可写；落库失败内部吞掉，恒返回 {ok:true} | client_log.py:36-55 |

## BE-INFRA 全局基础设施（非 router，但属验收范围）

| 编号 | 优先级 | 功能点 | 位置 | 预期行为 | 边界条件 | 备注 |
|---|---|---|---|---|---|---|
| BE-INFRA-01 | P1 | 统一响应信封 | EnvelopeRoute / EnvelopeJSONResponse（main.py:65-66） | 所有业务路由自动包 `{code,data,message}` 信封 | export/template 等返回 Response 的路由透传不包裹 | core/envelope.py |
| BE-INFRA-02 | P0 | JWT Bearer 鉴权 | get_current_user（core/security.py） | 全部受保护端点校验 token + DB 用户存在且未软删 | 无 token 401/1001；过期 403/1002 | — |
| BE-INFRA-03 | P1 | RBAC 角色 | require_admin / require_any_role | admin 路由与日志中心读守卫按 users.role 校验 | user/admin/auditor 三角色，落库 VARCHAR | core/enums.py UserRole |
| BE-INFRA-04 | P0 | 组合归属隔离依赖 | get_portfolio（app/common.py:20-31） | 非本人或不存在组合统一 404「组合不存在」不泄露存在性 | — | — |
| BE-INFRA-05 | P1 | 写操作级联重算 | RecalculationService（REG-06） | cashflow/trade/price/cashbalance/snapshot 写删后统一入口触发区间重建或级联 | 返回 recalculation{fromDate,affectedDays,skippedManualDays} 反馈 | data/router.py:1-8 注释 |
| BE-INFRA-06 | P2 | APScheduler 数据库驱动调度 | start_scheduler/reload_schedule（main.py:49-55; core/scheduler.py） | 受 SCHEDULER_ENABLED 控制，从 job_configs 加载 enabled 任务；增删改任务后 reload | 懒导入 apscheduler，未安装直接跳过 | — |
| BE-INFRA-07 | P2 | SPA 托管 + 深链回退 | serve_spa（main.py:126-145） | FRONTEND_DIR 配置且目录存在时启用；/api/* 未注册路由 404 不被 SPA 吞；防目录穿越 | 默认不启用 API-only | — |
| BE-INFRA-08 | P2 | OpenAPI Bearer 注入 | _custom_openapi（main.py:148-169） | schema 注入 JWT-auth 安全定义并对所有操作加 security | ⚠️ docs/openapi.json 为历史产物已过时，勿作依据 | — |

---

# 三、遗留 [待人工确认] 项 —— 裁决记录（2026-08-25 全部裁决完毕，清单就此锁定）

> 分析证据链见会话记录；缺陷修复与改进项已登记至 `docs/backlog.md`。

| #   | 关联编号         | 问题摘要                                                                                     | 裁决                                                                           | 去向                                       |
| --- | ------------ | ---------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------- |
| 1   | BE-HLTH-07   | `GET /api/token` 公开无鉴权签发 demo token 并自动建 demo 用户（health/router.py:75-92）                 | **删除端点**（安全面 P0 隐患，冒烟改走 /api/auth/login）                                     | 阶段 3 删除池                                 |
| 2   | BE-SCH-07    | 任务日志仅要求登录，同组其余端点均 require_admin，与 schedule.py:12 docstring 矛盾                            | 真缺陷                                                                          | 缺陷修复池（schedule.py:346 一行改 require_admin） |
| 3   | BE-PF-08     | prices/refresh-async fire-and-forget：重启丢任务、异常静默（portfolio/router.py:107-115）             | 接受现状锁定                                                                       | backlog 改进项                              |
| 4   | BE-UPL-01    | 头像上传手工信封疑被 EnvelopeRoute 二次包裹                                                            | **误报，已证伪**：envelope.py:61-67 `_is_envelope` 识别 int code 信封 dict、:74-76 透传不包裹 | 关闭                                       |
| 5   | FE-SET-06    | 设置页「即将上线」文案过时，FLOW-P0-06 软提示已实现（use-transactions.ts:125,175）                             | 文案缺陷                                                                         | 缺陷修复池（SettingsPage.vue:554 一行文案修正）       |
| 6   | FE-XIRR-05   | yearlyData 冗余过滤，柱状图实际用 aggregateByYear(seriesData)（XirrAnalysisPage.vue:129-133,242-243） | 死代码                                                                          | 阶段 3 第 2 轮删除池                            |
| 7   | BE-SNP-02/06 | 快照路由 `snap_date: date = None` 注解不规范（data/router.py:399,434）                              | 类型卫生问题                                                                       | 缺陷修复池（规范化注解）                             |

---

# 四、P0 承重墙清单（R1.2，自旧版验收表移植）

以下代码为 P0 链路主路径，**任何删除方案不得触碰**；确需改动须单独提案并人工批准：

| 层 | 文件 | 承重理由 |
| --- | --- | --- |
| 后端 | backend/app/core/security.py | JWT 签发与校验（认证边界） |
| 后端 | backend/app/modules/auth/router.py | 登录/注册/注销/恢复/改密改邮箱全套账号生命周期 |
| 后端 | backend/app/common.py | get_portfolio 归属隔离（L20 起），全部数据端点的越权防线 |
| 后端 | backend/app/finance_core/xirr.py、nav.py、holding.py | 计算内核（XIRR/净值/持仓） |
| 后端 | backend/app/services/recalculation.py | RecalculationService（L40 起）区间/级联重算引擎 |
| 后端 | backend/app/modules/data/router.py | 流水/交易/价格/余额/快照五类计算输入的写入口 |
| 后端 | backend/app/modules/portfolio/router.py | 组合删除与 DELETE /data 清空 8 表 |
| 后端 | backend/app/modules/data_transfer/router.py | import commit 单事务批量落库 |
| 后端 | backend/app/modules/internal/router.py | 物理清理（账号/头像） |
| 前端 | web/src/lib/api-client.ts | token 携带与会话失效全局拦截 |
| 前端 | web/src/stores/auth.store.ts | 认证态单一来源 |
| 前端 | web/src/router/index.ts | 认证守卫与回跳意图机制 |
| 前端 | web/src/modules/auth/ | 登录/注册/恢复表单链路 |

---

# 五、生成方式说明

- 后端以 `backend/app/main.py:97-123` include_router 的全部 26 个 router 对象为入口，逐文件通读 router 与关键 service 源码记录方法/路径/入参出参/错误码/副作用。
- 前端以 `web/src/router/index.ts` 注册的全部路由为入口，逐页通读页面组件源码记录交互元素、预期行为与空态/错误态/加载态。
- 业务码依据 `backend/app/core/enums.py` BusinessErrorCode（认证 1001-1009 / 校验 2000 / 业务 3001 / 计算 4001 / 服务器 5000）。
- 未参考旧版《功能验收表》，未使用 docs/openapi.json。

## P0 全量源码核对记录（2026-08-25）

68 条 P0 条目（前端 23 + 后端 45）逐条对照源码核验，核对范围含方法+路径、预期行为、边界条件错误码/HTTP 状态、证据行号：

| 结果 | 数量 | 明细 |
|---|---|---|
| ✅ 通过 | 65 | auth 全组（含 register EMAIL_TAKEN 1003/409 user.py:44-49；restore 1008/1009 user.py:76-104；改密/改邮箱 PASSWORD_WRONG 1004 仅限当前密码校验 :119-124,:149-154）、portfolio 8 条写入口（router.py:27-116）、data 六组 CRUD+重算反馈（cashflows :96-140 / resolve :170-183 / securities 删除 :197-211 / trades :247-271 含 trade.py 三处卖出硬校验 / prices :294-318 / balances :341-365 含 validate_date_not_future）、snapshots :405-437 + snapshot.py:113-129（未来日期校验+upsertManual+重算）、dividend 防跨组合/不触发重算（dividend/router.py 模块注释）、导入 preview/commit（dt/router.py:81-150，token 绑定 purpose/type/portfolio_id，2000/400）、internal X-Internal-Token 403、admin 提供方/接口增删（:249-268,311-351,382-393,488-499）、全量行情刷新（:395-420）、主数据批量删除/同步（:728+,802-810）、schedule 新建/删除校验链（:233-266,309-326）、日志批量删除 skipped 规则（log_center.py:306+,369-424）、INFRA JWT（security.py:69-106）与归属隔离（common.py:20-31） |
| 🔧 核对中修正 | 3 | BE-AUTH-02/03 密码错断言由 1004/400 改正为 UNAUTHORIZED 1001/401（防枚举统一口径，user.py:61-74）；BE-PORTFOLIO 小节标题 10→11 接口 |

- 本清单经人工裁决确认后于 2026-08-25 锁定；后续变更须显式记录原因（R1.1）。