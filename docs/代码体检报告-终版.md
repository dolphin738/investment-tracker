---
title: 代码体检报告（终版）
date: 2026-08-25
tags:
  - AI编程
  - 代码重构
  - 审计报告
aliases:
  - 终版体检报告
  - 三合一体检报告
parent: "[[AI代码清理与瘦身实施计划]]"
status: 待人工标注
---

# 代码体检报告（终版 · 三份合并）

> [!important] 执行声明与合并说明
> 本报告为阶段 2 三份审计文档（《代码体检报告》《代码体检报告2》《代码体检报告-2026-08-25》）的**合并终版**。
> **骨架**：以《代码体检报告-2026-08-25》（六维版，43 条）为准——其覆盖最广、证据最深、经抽验准确性最高；
> **补录**：并入前两份报告中六维版未覆盖的独有发现 **12 条（REP-044 ~ REP-055）**，全部重新编号避免冲突；
> **验证**：所有补录条目已于 2026-08-25 用 grep/glob 对当前工作树逐一复核（见各条"复核"标注），未通过核验的结论一律不收录（如报告2 称 papaparse"有真实引用"，经核验仅剩 vite.config 分包字符串，判定为误报剔除）。
> 本报告由 AI 只读审计产出，全程未修改任何项目源码。审计范围：`backend/app`、`web/src`、依赖清单与测试资产；已排除 `.venv`、`node_modules`、`dist`、`.git`。
> 部署形态基线：Docker 单镜像，后端监听 `0.0.0.0:3000` 并同源托管前端，属"个人/家庭内网自用、可能端口映射公网"形态，安全风险评级基于此。

## 发现索引（按风险等级降序）

| 编号 | 维度 | 简述 | 风险 |
| --- | --- | --- | --- |
| REP-001 | 安全隐患 | `GET /api/token` 未鉴权签发 JWT 并自动创建演示账户 | 高 |
| REP-002 | 安全隐患 | `JWT_SECRET` 危险默认值且启动不校验 | 高 |
| REP-003 | 安全隐患 | `LOCAL_COMMAND` 定时任务类型 = 内置任意命令执行通道（shell=True） | 高 |
| REP-004 | 废弃依赖 | httpx 被运行时使用却声明为 dev 依赖（反向废弃，已核实） | 高 |
| REP-005 | 缺失测试 | 后端 P0 功能点 10 条完全无测试覆盖（置顶） | 高 |
| REP-006 | 缺失测试 | 前端 P0 破坏性操作 4 条无 UI 测试覆盖 | 高 |
| REP-007 | 缺失测试 | P0 仅部分覆盖 6 条 | 高 |
| REP-008 | 安全隐患 | `INTERNAL_CLEANUP_TOKEN` 默认弱值，未认证可触发不可逆物理清理 | 中 |
| REP-009 | 安全隐患 | SSRF：HTTP 回调 url 与行情 provider base_url 无目标校验 | 中 |
| REP-010 | 安全隐患 | 公开注册 + 登录无限速 + 密码/邮箱零策略 | 中 |
| REP-011 | 安全隐患 | JWT 7 天有效期且无吊销机制 | 中 |
| REP-012 | 废弃接口 | `POST /api/portfolios/{id}/prices/refresh-async` 三方零调用 | 中 |
| REP-013 | 废弃接口 | 7 个后端端点前端无调用、仅测试使用 | 中 |
| REP-014 | 废弃依赖 | config.py 两个配置项已自我标注 deprecated | 中 |
| REP-015 | 冗余调用 | StorageService 抽象基类仅一个生产实现 [待人工确认] | 中 |
| REP-016 | 缺失测试 | P1 功能点缺口（按组归纳） | 中 |
| REP-044 | 重复模块 | overview/use-snapshots.ts 与 snapshot 版重复，vue-query key 双份硬编码 *†* | 中 |
| REP-045 | 重复模块 | overview/use-query-data.ts 与 analysis 版五件套重复且已行为分叉 *†* | 中 |
| REP-048 | 死代码 | 前端公共层 13 个死导出（lib/constants.ts + lib/types.ts） *†* | 中 |
| REP-052 | 重复模块 | installJsdomPolyfills 在 21 个测试文件逐份复制 + buildRouter ×2 *†* | 中 |
| REP-053 | 重复模块 | auth 内联 user dict ≥5 处（与验收表 createdAt 缺陷同源） *†* | 中 |
| REP-017 | 安全隐患 | 客户端日志端点可写入任意长度内容 | 低 |
| REP-018 | 安全隐患 | bootstrap_admin 随机密码打印到 stdout | 低 |
| REP-019 | 安全隐患 | CORS 具备通配+凭据的误配可能性（当前默认安全） | 低 |
| REP-054 | 安全隐患 | .env.example 携带真实熵密钥样例 *†* | 低 |
| REP-020 | 死代码 | PageSkeleton.vue 整文件无引用 | 低 |
| REP-021 | 死代码 | CardSkeleton.vue 传递性死代码（唯一引用方为 REP-020） | 低 |
| REP-022 | 死代码 | LoadingSpinner.vue 整文件无引用 | 低 |
| REP-023 | 死代码 | ui/alert 目录 4 文件零外部导入（与 alert-dialog 是不同组件族） | 低 |
| REP-024 | 死代码 | ui/select 三个未用子组件 | 低 |
| REP-025 | 死代码 | DialogTrigger.vue / DialogClose.vue 仅被 index.ts 导出 | 低 |
| REP-026 | 死代码 | AlertDialogTrigger.vue 仅被 index.ts 导出 | 低 |
| REP-027 | 死代码 | lib/utils.ts 的 isStale() 函数调用计数为 0 | 低 |
| REP-028 | 死代码 | core/date_utils.py 的 now_app_tz() 全仓仅定义处 1 次命中 | 低 |
| REP-046 | 死代码 | calculation/router.py `_shares_at()` 全仓仅定义处命中 *†* | 低 |
| REP-049 | 死代码 | 12 个"过度导出"（降级 export 关键字，类型卫生） [待人工确认] *†* | 低 |
| REP-050 | 死代码 | stringCodec 仅测试引用 [待人工确认] *†* | 低 |
| REP-047 | 冗余调用 | security-price 模块悬空转出口 useUpsertSecurityPrice *†* | 低 |
| REP-029 | 重复模块 | NavTrendChart 组件双份实现（overview 内联版 vs charts 纯函数版） | 低 |
| REP-030 | 重复模块 | XirrTrendChart 组件双份实现（同上结构） | 低 |
| REP-031 | 重复模块 | AxisTooltipParam 接口四处逐字复制（3 charts + total-asset-trend-chart） | 低 |
| REP-032 | 重复模块 | tooltip extraCssText 样式串 3 处以上复制 | 低 |
| REP-033 | 重复模块 | xirr/yearly 两图 tooltip formatter 与百分比轴配置重复 | 低 |
| REP-034 | 重复模块+冗余调用 | formatDateTime 在 SchedulePage 与 LogCenterPage 逐字重复 | 低 |
| REP-035 | 重复模块+冗余调用 | toIsoDate 与 formatDate 平行实现 | 低 |
| REP-036 | 重复模块 | asset_valuation.py 两组同构 upsert 骨架 | 中低 |
| REP-037 | 重复模块 | dividend/snapshot 分页样板与 common.paginate 平行实现 | 低 |
| REP-038 | 重复模块 | serializers.py 与 schemas_resp.py 字段清单双维护（建议加护栏而非合并） | 低 |
| REP-039 | 废弃依赖 | date-fns 零引用 | 低 |
| REP-040 | 废弃依赖 | papaparse 零引用（已核实：仅剩 vite.config 分包字符串） | 低 |
| REP-041 | 废弃依赖 | @types/papaparse 连带废弃 | 低 |
| REP-042 | 废弃接口 | e2e mock 存在永不命中的 transactions 路由规则 | 低 |
| REP-051 | 废弃接口 | api.ts 空壳导出 paths/operations（关联 openapi.json 过时决策） [待人工确认] *†* | 低 |
| REP-055 | 工作区残留 | 游离文件（未跟踪 log/txt/json，不进仓库，本地卫生） *†* | 低 |
| REP-043 | 缺失测试 | P2 功能点缺口（备查清单） | 低 |

> \*†\* = 自前两份报告补录的条目（其余为六维版原文）。全部补录条目已于 2026-08-25 复核。

---

## 一、高风险发现

### REP-001 安全隐患：`GET /api/token` 未鉴权签发 JWT，并自动创建演示账户

- 维度：安全隐患（遗留调试端点进入生产）
- 位置：backend/app/modules/health/router.py:75-92（注册于 backend/app/main.py:97）
- 证据：

```python
@router.get("/token")
async def issue_demo_token(db: AsyncSession = Depends(get_db)) -> dict:
    # 仅用于冒烟测试便捷获取 token（真实签发在 auth 模块）。
    user = (await db.execute(select(User).where(User.id == "demo-user-id"))).scalar_one_or_none()
    if user is None:
        db.add(User(id="demo-user-id", email="demo@example.com",
                    password_hash=hash_password("demo-pass"), name="Demo"))
        await db.commit()
    return {"token": create_access_token("demo-user-id", "demo@example.com")}
```

- 影响范围：该路由随 health.router 无条件挂载，生产 Docker 镜像同样生效。任何能访问 3000 端口的人无需凭据即可获得合法登录态 token，进而调用全部普通用户接口；若与 demo 邮箱发生用户冲突或误提权，影响升级。
- 建议动作：删除 `/api/token` 端点（契约冒烟改由 pytest 内直接调用 `create_access_token`），或加环境开关仅测试构建启用。
- 风险等级：高
- 关联：功能验收表 BE-HLTH-07 已裁决"阶段 3 删除"，与本条互相印证。

### REP-002 安全隐患：`JWT_SECRET` 危险默认值且启动不校验强度

- 维度：安全隐患（硬编码默认密钥）
- 位置：backend/app/core/config.py:23（JWT_SECRET）、config.py:28-30（DATABASE_URL 弱默认）
- 证据：`JWT_SECRET: str = "change-me-in-prod"`；`get_settings()` 直接返回默认值；docker-entrypoint.sh 不做检查，Dockerfile 运行命令为 `--env-file backend/.env`，漏传即静默回退默认。（注：`.env.example` 注释虽要求 ≥32 字节随机串，但 Settings 类无任何启动校验。）
- 影响范围：部署者忘配 .env 时密钥为人尽皆知的 `"change-me-in-prod"`，任何人可离线伪造任意用户（含已知 admin 用户 id）的 JWT，完全接管系统；与 REP-001 叠加构成未认证到服务器沦陷的完整链路。
- 建议动作：启动时校验——`JWT_SECRET` 等于默认值或长度不足则拒绝启动（至少 FATAL 日志告警）；同理校验 DATABASE_URL 不含弱默认口令。
- 风险等级：高

### REP-003 安全隐患：`LOCAL_COMMAND` 定时任务类型是内置任意命令执行通道

- 维度：安全隐患（设计特性型命令注入面）
- 位置：backend/app/core/scheduler.py:171-194、backend/app/modules/admin/schedule.py:44-50
- 证据（2026-08-25 已对当前工作树核实）：scheduler.py:183-185 为 `subprocess.run(command, shell=True)`，command 来自 `params.get("command")`：

```python
command = (params.get("command") or "").strip()
result = await asyncio.to_thread(
    subprocess.run, command,
    shell=True, stdout=subprocess.PIPE, stderr=subprocess.STDOUT,
    timeout=_LOCAL_COMMAND_TIMEOUT, check=False, text=True, errors="replace",
)
```

admin/schedule.py 将 LOCAL_COMMAND 标记为 creatable=True，POST /api/admin/tasks 可创建并 trigger 立即执行。

- 影响范围：权限链本身收口正确（create_task/trigger_task 均 require_admin，已逐一核对），单独看不构成漏洞；但它是产品内置 RCE 原语——admin 会话一旦被劫持（token 泄漏或 REP-002 密钥伪造），攻击者一步拿到容器内 shell，使所有 admin 凭据类漏洞后果升格为服务器沦陷。另叠加已知缺陷 BF-01（`GET /api/admin/tasks/{id}/logs` 仅挂 get_current_user），普通登录用户可读到 LOCAL_COMMAND 完整 stdout/stderr，形成信息泄露放大面。
- 建议动作：增加环境开关（如 ENABLE_LOCAL_COMMAND 默认 false）；或执行白名单目录；BF-01 按 backlog 既有一行修复改为 require_admin；或在运维文档显著标注信任模型。
- 风险等级：高（链式放大）

### REP-004 废弃依赖：httpx 被运行时使用却声明为 dev 依赖【反向废弃，2026-08-25 已核实】

- 维度：废弃依赖与接口（应升格而非删除）
- 位置：backend/pyproject.toml dev 组声明（`httpx==0.28.1` 位于 `[project.optional-dependencies].dev`）
- 复核证据：`scheduler.py:26` 与 `services/market_data_sync.py:33` 均有运行时顶层 `import httpx`（scheduler 被 main.py 导入，进程启动即加载）；另有测试侧 conftest.py:123、helpers.py:9。
- 影响范围：若以不含 dev 组方式安装（如生产精简安装），应用启动即 ImportError。
- 建议动作：将 `httpx==0.28.1` 移入 `[project.dependencies]`；dev 组保留 httpx2 即可。
- 风险等级：高

### REP-005 缺失测试：后端 P0 功能点 10 条完全无覆盖（置顶）

对照 docs/功能验收表.md（阶段1产物，247 条功能点），以下 P0 条目在 backend/tests 31 个测试文件中零命中：

| # | 验收表条目 | 判定证据 |
| --- | --- | --- |
| 1 | BE-AUTH-03 账户恢复（POST /api/auth/account/restore，1008/1009/1001 三错误分支） | 全目录搜索 `account/restore`、`pending_deletion`、`1008` 仅命中证券代码子串误匹配；前端 login-form.test.ts 反而测全了 1007 分支 |
| 2 | BE-CF-04 编辑流水（PATCH cashflows/{id}） | 展开 23 处 `client.patch(` URL 清单，无任何 cashflows PATCH；DELETE 有覆盖唯独 PATCH 空缺 |
| 3 | BE-PRC-03 编辑价格（PATCH security-prices/{id}） | security-prices 测试仅有 POST 建数据与 GET 断言 |
| 4 | BE-PRC-04 删除价格（DELETE security-prices/{id}） | delete URL 清单无该路径 |
| 5 | BE-CB-03 编辑余额（PATCH cash-balances/{id}） | patch URL 清单无此路径（POST/DELETE 有） |
| 6 | BE-SNP-04 编辑快照变手工（PATCH snapshots/{id}） | snapshots 有 POST/reset/DELETE/GET 过滤测试，patch 无 |
| 7 | BE-PF-08 行情异步刷新（POST prices/refresh-async，202 契约） | 搜索 `refresh-async` 在 backend/tests 零命中 |
| 8 | BE-SCH-03 新建普通任务（POST /api/admin/tasks 校验链） | 搜索 `admin/tasks` 在 backend/tests 零命中——BE-SCHEDULE 全部 7 条接口无任何测试触及 |
| 9 | BE-SCH-05 删除任务（DELETE /api/admin/tasks/{id}） | 同上整组零命中 |
| 10 | BE-LGC-03 批量删除日志（DELETE /api/admin/logs，skipped 规则 + auditor 403） | test_log_center.py 全文搜索 delete 零命中 |

结构性规律：五类数据 CRUD 的 PATCH 半边系统性缺失（POST/DELETE 有、PATCH 几乎全无）；行情同步域与管理端任务/日志域整体无测试。
建议动作：按上述三簇批量补 API 集成测试（属阶段 3 第 5 轮"补 P0 测试"，非删除动作）。
风险等级：高

### REP-006 缺失测试：前端 P0 破坏性操作 4 条无 UI 覆盖

| # | 验收表条目 | 判定证据 |
| --- | --- | --- |
| 1 | FE-SNAP-04 快照删除记录行操作 | 搜索 SnapshotsPage/SnapshotList 在所有 *.test.ts 零命中，仅 snapshot-form.test.ts 覆盖表单 |
| 2 | FE-SNAP-05 重置撤销手工行操作 | 同上 |
| 3 | FE-SET-11 清空当前组合数据 | settings-page.test.ts 中 clearPortfolioData/deleteAccount 仅作为 vi.mock 存根出现，「名称精确匹配才可确认」守卫逻辑无用例 |
| 4 | FE-SET-12 注销账户 | 同上——这是全应用最具破坏性的两个入口 |

建议动作：组件测试优先覆盖「名称不匹配禁用确认」与「匹配后才调 API」两分支；破坏性行操作建议补 e2e。
风险等级：高

### REP-007 缺失测试：P0 仅部分覆盖 6 条

BE-PF-07（prices/sync 有 service 层测试、HTTP 端点层无）、BE-ADM-13（admin 全量刷新编排端点无）、FE-GLOBAL-01（认证守卫只测了"登录后放行"、未测"未登录被 replace 到 /login"及回跳深链）、FE-OVW-06/07（弹窗入口挂载链路仅 mock 形式存在）、FE-SNAP-03（编辑变手工，且其依赖的 BE-SNP-04 本身无测试）。判定方法均为展开测试内全部 HTTP 调用 URL 与 vi.mock 清单比对。
风险等级：高（P0 口径）

---

## 二、中风险发现

### REP-008 安全隐患：INTERNAL_CLEANUP_TOKEN 默认弱值

- 位置：backend/app/core/config.py:54、backend/app/modules/internal/router.py:28-41
- 证据：`INTERNAL_CLEANUP_TOKEN: str = "change-me-internal"`；`_assert_internal_token` 用 `!=` 比较；对应端点 POST /api/internal/cleanup/accounts 触发 CleanupService.physical_purge() 物理清除。
- 影响范围：端口暴露且未改默认值时，任何人携带默认 token 即可触发不可逆物理清除（软删除过保留期的数据永久消失）。时序侧信道在本部署形态下不可实际利用，不单独计项。
- 建议动作：启动检测默认值并拒绝启动/告警；比较改用 `secrets.compare_digest`。
- 风险等级：中

### REP-009 安全隐患：SSRF（HTTP 回调 url 与 provider base_url 无目标校验）

- 位置：backend/app/core/scheduler.py:197-209（HTTP_CALLBACK 任务类型）、backend/app/services/market_data_sync.py:431-468、backend/app/modules/admin/router.py:51-58
- 证据：`url = (params.get("url") or "").strip()` 后直接 `client.request(method, url)`；provider 的 base_url 仅校验"是非空字符串"即拼入请求 URL。
- 影响范围：两者均 admin-only 配置，非匿名可利用；但结合 REP-001/REP-002 获得的会话即可让服务器探测内网、命中云元数据地址、经 job_run_logs 带出部分响应信息。单机自部署形态利用价值中等。
- 建议动作：scheme 白名单 http/https；可选禁止环回/链路本地网段（provider 允许私网时提供显式开关）；timeout 上限约束。
- 风险等级：中

### REP-010 安全隐患：公开注册 + 登录无限速 + 密码/邮箱零策略

- 位置：backend/app/modules/auth/router.py:27-29、backend/app/schemas.py:20-23、backend/app/services/user.py:61-74
- 证据：`email: str`（非 EmailStr）、`password: str`（无长度下限）；全项目搜索限流相关关键词仅行情接口有限流，登录失败无限速。
- 影响范围：服务暴露公网时可无限注册账户、持续爆破登录（唯一减速是 bcrypt cost）；纯内网自用基本不可利用——据此评中而非高。
- 建议动作：登录失败按 IP+账号限速；注册加环境开关供单用户部署关闭；email 改 EmailStr、password 加最小长度。
- 风险等级：中

### REP-011 安全隐患：JWT 7 天有效期且无吊销机制

- 位置：backend/app/core/config.py:25、backend/app/core/security.py:31-46
- 证据：`ACCESS_TOKEN_EXPIRE_MINUTES: int = 60 * 24 * 7`；get_current_user 仅校验签名/过期 + 用户存在未软删除，无 token 版本校验；改密后旧 token 依旧有效。
- 影响范围：token 泄漏后有 7 天窗口且无法通过改密止损。角色降权有库级实时校验防护（这点做得好）。
- 建议动作：User 表加 token_version（或以 password_hash 前 8 字符作 claim 比对），改密/注销时递增；有效期缩至 1 天配刷新。
- 风险等级：中

### REP-012 废弃接口：refresh-async 端点三方零调用

- 维度：废弃依赖与接口
- 位置：backend/app/modules/portfolio/router.py:97
- 证据：关键词 `refresh-async` 在 web/src、web/e2e、backend/tests、backend/scripts 全部零命中（唯一出现即 router 定义行）；前端 portfolio-price.api.ts 仅封装同步版 `/prices/sync` 与 `/prices/sync-status`。
- 影响范围：异步刷新链路（202 + 轮询）整体无人消费。
- 建议动作：确认产品无规划后删除该端点及其 service 支撑代码。
- 风险等级：中
- 关联裁决：验收表 BE-PF-08 将其标为 P0 且无测试（REP-005 第 7 条）。两条联动二选一：删端点（同时从验收表移除 BE-PF-08），或保留并补 202 契约测试。

### REP-013 废弃接口：7 个后端端点前端无调用、仅测试使用

| 端点 | 定义位置 | 备注 |
| --- | --- | --- |
| GET /api/portfolios/comparison | aggregation/router.py:52 | 仅 test_api_aggregation.py 使用；comparison 字面路由还约束了 main.py:99 注册顺序 |
| GET /api/portfolios/{id}/summary | aggregation/router.py:60 | 前端只用列表版 /portfolios/summary |
| GET /api/portfolios/{id}/metrics/drawdown | aggregation/router.py:77 | 仅测试使用 |
| POST /{id}/recalculate 与 /recalculate-range | calculation/router.py:338,354 | 业务重算由后端服务层级联完成，前端从不直调 |
| GET /{id}/nav/history | calculation/router.py:440 | 仅测试使用 |
| GET /{id}/xirr/history | calculation/router.py:458 | 仅测试使用 |

- 证据：上述路径在 web/src/api/*.ts 与 e2e/fixtures/mock-api.ts 路由表中均无对应请求构造。
- 影响范围：属"测试专用/预留 API"，非死代码，构成 API 表面积维护成本。
- 建议动作：与团队确认是否保留对外契约；若无前端路线图可标记 internal 或列入 backlog 观察。另有 5 个低置信条目标注 [待人工确认]，建议结合 Swagger 访问日志裁决。
- 风险等级：中

### REP-014 废弃依赖：config.py 两个配置项已自我标注 deprecated

- 位置：backend/app/core/config.py:64-73
- 证据：pydantic-settings 字段带 `deprecated="Use job_configs (MARKET_DATA_SYNC) + SCHEDULER_ENABLED instead"` 等（`QUOTE_SYNC_SCHEDULER_ENABLED` / `QUOTE_SYNC_SCHEDULER_CRON` 旧开关；2026-08-26 复核更正字段名）。
- 影响范围：运行时仅产生弃用告警，功能已被 job_configs 取代。
- 建议动作：确认 .env 生产实例均已迁移后移除字段。
- 风险等级：中

### REP-015 冗余调用：StorageService 抽象基类仅一个生产实现 [待人工确认]

- 位置：backend/app/storage/base.py:12-27、backend/app/storage/factory.py:12-19
- 调用链：UploadService/UserService 构造器 storage 参数 -> get_storage_driver() -> LocalDiskStorage（唯一实现）
- 证据：全仓搜索 LocalDiskStorage 为唯一实现；生产消费方 2 处；测试未注入第二个 fake 实现。扩展意图证据并存：factory 注释明示 cos/s3 预留分支、构造器保留注入口、文件头注明对标 NestJS 版同名抽象。
- 原生替代方案（若确认放弃对象存储计划）：删除 ABC 与工厂，直接实例化 LocalDiskStorage，测试用 monkeypatch 替换。
- 建议动作：人工确认产品路线图是否含 COS/S3；无则删（约减 35 行），有则保留现状。涉及架构决策，不建议自动执行。
- 风险等级：中

### REP-016 缺失测试：P1 功能点缺口（按组归纳）

| 组 | 条目 | 证据摘要 |
| --- | --- | --- |
| BE-QS | QS-01/02/03 行情同步配置三接口整组无测试 | grep quote-sync 在 backend/tests 仅命中表名字符串 |
| BE-PF | PF-09 sync-status、PF-11 设默认 toggle | grep /default 仅命中 archive 用例 |
| BE-SEC | SEC-03 编辑标的 PATCH | 23 处 patch URL 清单无 securities |
| BE-DT | DTX-01/04 导出与模板 | csv 有测，xlsx 格式无 |
| FE-GLOBAL | GLOBAL-04 组合切换器、GLOBAL-07 PreferenceBootstrap、GLOBAL-10 重定向 | 相关组件/composable 在测试中零命中 |
| FE-HOLD/CF | HOLD-01 页签持久化、CF-07 当前余额、CF-10 软提示 | composable/事件常量在测试中零命中 |
| FE-ACC | ACC-07 默认星标、ACC-10/11 行情自动同步 | AutoSyncCard 无组件测试 |
| FE-SETTINGS | SET-05 退出登录 | settings-page 用例全为偏好主题 |
| FE-ADMIN-FIN | ADM-06 股票列表面板 | 仅 api 层有序列化测试，StockListPanel 组件无 |
| BE-AUTH | AUTH-05 GET profile [待人工确认] | test_api_auth 只有 PATCH profile |

建议动作：随阶段 3 各轮次穿插补齐，优先 BE-QS 组（涉及定时同步行为正确性）。
风险等级：中

### REP-044 重复模块：overview/use-snapshots.ts 与 snapshot 版核心符号重复【补录】

- 位置：web/src/modules/overview/composables/use-snapshots.ts（36 行，全文）⇄ web/src/modules/snapshot/composables/use-snapshots.ts:40-90+（201 行）
- 复核证据：两文件均存在（glob 确认）；`snapshotsKey` 工厂逐字符一致（都返回 `['snapshots', portfolioId, query]`），`useSnapshots` 函数体语义一致，overview 版是 snapshot 版真子集；其文件头自述"平移列表 query 部分，mutation 待批次补齐"——该批次早已完成，桥接理由消失。
- 影响范围：vue-query 缓存失效依赖 query key 字面一致性，两处独立硬编码 key，一旦单侧改动即出现"快照 mutation 后概览页不刷新"类隐性缺陷。
- 建议动作：DashboardPage 改 import snapshot 版，删除 overview 版整文件，约 **-36 行**。
- 风险等级：中

### REP-045 重复模块：overview/use-query-data.ts 与 analysis 版五件套重复且已行为分叉【补录，2026-08-26 复核更正】

- 位置：web/src/modules/overview/composables/use-query-data.ts（147 行）⇄ web/src/modules/analysis/composables/use-query-data.ts（125 行）（两文件均已 glob 确认存在）
- 证据：各自实现同名**五件套** `useXirrSeries` / `useNavSeries` / `useLatestXirr` / `useLatestNav` / `useYearStartXirr`，queryKey 结构与调用的 api 完全相同，且**已经发生行为分叉**：analysis 版对 portfolioId 为空做了 `'disabled'` key 占位守卫（含 useYearStartXirr，analysis 版 :93-99），overview 版把 `null` 直接写进 key——同一份数据在两个页面可能落入不同 query key 条目，缓存互不命中且失效不联动。overview 版独有的 `useNavTotalAssetMap` 不在重复范围内。
- 建议动作：五件套收敛到单一模块（建议留在 analysis 版并以其为准补齐），overview 版仅保留独有 hooks；预计 **-45 行**。
- 风险等级：中

### REP-048 死代码：前端公共层 13 个死导出【补录，抽样复核通过】

- 位置与清单（全局搜索 src+e2e 零引用，定义文件内仅出现 1 次）：

| 符号 | 文件 | 类型 |
| --- | --- | --- |
| API_PREFIX | web/src/lib/constants.ts | 常量 |
| LAST_ROUTE_KEY | web/src/lib/constants.ts | 常量 |
| HOLDINGS_QUERY_KEYS | web/src/lib/constants.ts | 常量 |
| OVERVIEW_QUERY_KEYS | web/src/lib/constants.ts | 常量 |
| getDefaultDateRange | web/src/lib/constants.ts | 函数 |
| PaginationQuery | web/src/lib/types.ts | 类型 |
| DateRangeQuery | web/src/lib/types.ts | 类型 |
| SUCCESS_MESSAGE | web/src/lib/types.ts | 常量 |
| SUCCESS_CODE | web/src/lib/types.ts | 常量 |
| ACCOUNT_RETENTION_MS | web/src/lib/types.ts | 常量 |
| EXPORT_TYPES | web/src/lib/types.ts | 常量 |
| IMPORT_TYPES | web/src/lib/types.ts | 常量 |
| exchangeLabel | web/src/lib/types.ts:235 | 函数 |

- 复核证据（2026-08-25 抽样 4 项）：API_PREFIX / LAST_ROUTE_KEY / SUCCESS_CODE / ACCOUNT_RETENTION_MS 在 *.ts/*.vue 全局检索均仅命中定义行本身，零引用成立。**注意**：exchangeLabel 外部虽零引用，但 EXCHANGE_LABELS 常量在 StockListPanel.vue:14 有引用，**勿连带删除**。
- 建议动作：阶段 3 轮次 2 删除，预计约 **-60 行**。
- 风险等级：中

### REP-052 重复模块：测试辅助函数在测试文件间逐份复制【补录，计数已核实 = 21】

- 位置：`function installJsdomPolyfills` 在 **21 个** `web/src/modules/**/__tests__/*.test.ts` 各自定义（2026-08-25 glob 计数恰好 21，每份约 10-15 行 pointer/scrollIntoView 等 jsdom 补丁，实现完全一致）；另有 `function buildRouter` 在 auth 模块 login-form.test.ts 与 register-form.test.ts 重复 2 处。
- 建议动作：提取到共享 test-utils（如 web/src/test-utils/jsdom-polyfills.ts），23 处改 import；预计净省 **200+ 行**；属阶段 3 第 3 轮（纯测试侧，实际风险低）。
- 风险等级：中

### REP-053 重复模块：auth 路由内联 user dict 构造 ≥5 处【补录，2026-08-26 复核更新缺陷状态】

- 位置：backend/app/modules/auth/router.py:87/110/131/154/178 等（register/login/restore/me/改密/改邮箱各自内联构造 `"id": u.id` 字典）
- 证据与状态更新：重复本身属实；但 2026-08-26 复核发现当前代码中这 5 处内联 dict **均已带 `createdAt`**（:94/:117/:138/:161/:185），《功能验收表》4.3 所裁决的"7 处缺 createdAt"缺陷疑已被此前修复批次消化——收敛前请与验收表 owner 确认该缺陷是否仍开放，避免重复排期。
- 建议动作：新增 serialize_user 共享函数（对齐 serializers.py 风格）消除重复；若验收表缺陷已关闭则本条降为纯重构项，**需人工排期，非删除动作**。
- 风险等级：中

---

## 三、低风险发现

### 安全类

| 编号 | 位置 | 证据与攻击场景 | 建议 |
| --- | --- | --- | --- |
| REP-017 | backend/app/modules/client_log.py:26-34 | message 无 max_length，detail 为任意 JSON 直落 Text 列 | schema 加长度上限（message ≤2000，detail 序列化 ≤16KB） |
| REP-018 | backend/scripts/bootstrap_admin.py:114-121 | 随机管理员密码 print 到 stdout，Docker/CI 场景进日志聚合 | 打印处改为仅提示见凭据文件 |
| REP-019 | backend/app/main.py:71-77、core/config.py:46 | CORS_ORIGINS 允许填 `*` 且 allow_credentials=True；当前默认 localhost:5173 安全；认证走 Bearer 非 Cookie，CSRF 不成立 | 启动检测通配+凭据组合打告警 |
| REP-054 | backend/.env.example:5 | 携带真实熵密钥样例 `YeYdWyWAvxCrQ169N5ITu2ZlwqRmsoTIH6O7v9Li2-E`，存在被直接复制到生产的惰性风险 | 替换为 `CHANGE_ME_TO_RANDOM_32BYTE_SECRET` 式占位符（可随阶段 3 第 1 轮顺带） |

### 死代码类（前端为主，均可安全删除）

| 编号      | 位置                                                                                                                                                                                                                                                                                    | 证据（确认方式）                                                                                                    | 可删行数                          |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ----------------------------- |
| REP-020 | web/src/components/common/PageSkeleton.vue                                                                                                                                                                                                                                            | 全项目搜 PageSkeleton/page-skeleton 仅命中自身；无 unplugin 自动导入机制（已核实 package.json 与 main.ts）                         | 35                            |
| REP-021 | web/src/components/common/CardSkeleton.vue                                                                                                                                                                                                                                            | 全项目仅 2 处命中：自身定义 + REP-020 的相对导入（唯一引用方本身不可达）                                                                 | 15                            |
| REP-022 | web/src/components/common/LoadingSpinner.vue                                                                                                                                                                                                                                          | 全项目仅命中自身文件                                                                                                  | 38                            |
| REP-023 | web/src/components/ui/alert/ 目录（Alert/AlertDescription/AlertTitle/index.ts 共 4 文件）                                                                                                                                                                                                    | 搜索导入语句所有命中均为 ui/alert-dialog（不同组件族，12 处使用）；ui/alert 目录零外部导入                                                 | 73                            |
| REP-024 | web/src/components/ui/select/SelectGroup.vue、SelectLabel.vue、SelectSeparator.vue                                                                                                                                                                                                      | 全部 11 处 select 导入块只用 Select/Content/Item/Trigger/Value 五件套；ScrollUp/DownButton 被 SelectContent 内部引用不算死代码勿误删 | 35                            |
| REP-025 | web/src/components/ui/dialog/DialogTrigger.vue、DialogClose.vue                                                                                                                                                                                                                        | 业务对话框均 v-model:open 受控驱动；DialogContent 内部用 reka-ui 原生 DialogClose；本地两组件仅被 index.ts 导出                       | 22                            |
| REP-026 | web/src/components/ui/alert-dialog/AlertDialogTrigger.vue                                                                                                                                                                                                                             | 9 处使用方均受控 open 方式触发                                                                                         | 11                            |
| REP-027 | web/src/lib/utils.ts isStale() 函数（约 L138-153）                                                                                                                                                                                                                                         | 其余命中全是 FreshnessInfo 接口同名布尔字段，函数调用计数 0；新鲜度判断已由后端下发取代                                                        | 16                            |
| REP-028 | backend/app/core/date_utils.py now_app_tz()                                                                                                                                                                                                                                           | 全 backend 含 tests 搜索仅定义处 1 次命中；同文件 today_app_tz 被 6+ 处广泛使用形成对比                                              | 3                             |
| REP-046 | backend/app/modules/calculation/router.py:307 `_shares_at()`                                                                                                                                                                                                                          | 2026-08-25 复核：全 app/ 搜索仅定义处 1 次命中；nav 聚合已由 `_agg` 内联完成 shares 聚合                                            | 4                             |
| REP-049 | 12 个"过度导出"[待人工确认]：CronParts/parseCron/cronNextRun（cron.ts）、ClientLogLevel/ClientLogPayload（log-reporter.ts）、MONEY_RE/FreshnessKind/MoneyOptions（types.ts）、UrlCodec（url-query.ts）、FormatOptions/FormatCurrencyOptions/ManualDiffStats（utils.ts）、EntryButtonKey（entry-button-labels.ts） | 外部零引用、文件内部有真实调用                                                                                             | 0（去 export 关键字，类型卫生非删除；优先级最低） |
| REP-050 | web/src/lib/url-query.ts stringCodec [待人工确认]                                                                                                                                                                                                                                          | 外部 2 处引用均在测试文件                                                                                              | 若视为测试基础设施契约可保留；否则随轮次 2 删除     |

### 重复模块与冗余调用类

| 编号 | 位置 A vs 位置 B | 相似点与差异 | 可减行数 | 风险 |
| --- | --- | --- | --- | --- |
| REP-029 | modules/overview/components/NavTrendChart.vue:53-163（182 行，内联 option）vs components/charts/NavTrendChart.vue:40-47（79 行，调纯函数） | props 契约一致；tooltip formatter 逐字相同；B 为演进超集（option 抽纯函数有单测锁定 + aria 读屏摘要），A 无障碍缺失。DashboardPage 用 A，NavAnalysisPage 用 B | ~180 | 低 |
| REP-030 | modules/overview/components/XirrTrendChart.vue:42-111（130 行）vs components/charts/XirrTrendChart.vue:36（纯函数版） | 与 REP-029 同构：formatter/extraCssText 一致，B 多 aria 摘要 | ~128 | 低 |
| REP-031 | nav-trend-chart.ts:20-26 vs xirr-trend-chart.ts:17-23 vs yearly-bar-chart.ts:23-29（另有第 4 份：overview/features/total-asset-trend-chart.ts:28 且为 export） | AxisTooltipParam 接口逐字复制（含注释）；2026-08-26 复核补录第 4 份生产副本 | ~18 | 低 |
| REP-032 | nav-trend-chart.ts:100-106、xirr-trend-chart.ts:59-65、yearly-bar-chart.ts:71-77 | extraCssText 样式串拼接逐字一致 | ~15 | 低 |
| REP-033 | xirr-trend-chart.ts:66-96 vs yearly-bar-chart.ts:78-107 | tooltip formatter 与百分比 yAxis formatter/splitLine 逐字一致（splitLine 在 4 个 ts 文件共 7 次） | ~20 | 低 |
| REP-034 | SchedulePage.vue:395-401 vs LogCenterPage.vue:326-332 | formatDateTime 8 行逐字相同；根因是 utils 仅有 dates 无 datetime。收敛到 lib/utils.ts 新增 formatDateTime | ~10 | 低 |
| REP-035 | lib/constants.ts:97-102 toIsoDate vs lib/utils.ts:24-50 formatDate('yyyy-MM-dd') | 同一日期转字符串算法维护两份；建议 toIsoDate 内部委托 formatDate 保留导出名，唯一行为变化是无效 Date 输入从 NaN 字符串变为 '-' | ~6 | 低 |
| REP-036 | asset_valuation.py:160-192 upsertManual vs 194-222 resetToDerived | "existing 则原地改 7 字段否则新建"骨架同构；字段来源与 source 常量不同。抽私有 _upsert_snapshot 辅助 | ~18 | 中低 |
| REP-037 | common.py:34-52 paginate vs services/dividend.py:49-59、snapshot.py:51-62 | count 子查询 + limit/offset 完全同款；差异在 dict 信封还是元组。在 services/base.py 下沉 paged() 原语 | ~14 | 低 |
| REP-047 | web/src/modules/security-price/composables/use-security-prices.ts:40 悬空转出口 | 2026-08-25 复核：`export { useUpsertSecurityPrice } from '@/modules/holdings/...'`，唯一消费方 InlinePriceEditor.vue:13 直接从 holdings 模块导入，经转出口消费方 0 命中 | 2 | 低 |

REP-038 特别说明（serializers.py vs schemas_resp.py 双维护）：**不建议合并实现**（强行合并会破坏 wire 契约，若合并风险为高），建议在现有 test_contract.py 增加"serialize_x 输出键集合 == XxxOut.model_fields.keys()"断言作为防漂移护栏，让字段漂移在 CI 阶段失败。

### 废弃依赖与残留类

| 编号 | 位置 | 证据 | 建议 |
| --- | --- | --- | --- |
| REP-039 | web/package.json:22 date-fns ^3.3.0 | 全 web 目录搜索仅 2 处命中：声明本身 + vite.config.ts:54 manualChunks 死分支；src/e2e/scripts 零 import | 移除依赖并删 vite.config.ts:54 死分支 |
| REP-040 | web/package.json:25 papaparse ^5.4.1 | 2026-08-25 复核：大小写不敏感搜 papa 仅命中 vite.config.ts:46 分包字符串；零实际 import（疑 Vue3 迁移中 CSV 改走后端后遗留） | 移除依赖并删 vite.config.ts:46 死分支 |
| REP-041 | web/package.json:40 @types/papaparse | 宿主包已零引用（REP-040），类型包连带废弃 | 随 REP-040 一并移除 |
| REP-042 | web/e2e/fixtures/mock-api.ts:251 | 规则匹配 /transactions，而前端实际调用 /cashflows，后端亦无 transactions 端点（React 版命名迁移遗留） | 删除该 mock 行 |
| REP-051 | web/src/types/api.ts:5（paths）/ :1007（operations）空壳导出 [待人工确认] | 文件头注明 Generated from docs/openapi.json；components 在用，paths/operations 零引用；docs/openapi.json 已知严重过时；行号经 2026-08-26 复核更正 | 生成文件不应手改；结合"重新生成或停止维护 openapi.json"一并决策 |
| REP-055 | 根目录游离文件：_fullfinal.log、vitest-run.log、vue-tsc-full*.log（3 份）、web/tsc.log；dev-scripts/ 杂物（"新建 文本文档.txt"、akshare 快照 json、_repro_schedule.mjs 等） | git ls-files 零跟踪；.gitignore 已忽略 | 本地磁盘清理（不影响仓库），按文件保护规则移入回收站 |

### 缺失测试 P2 类（备查）

REP-043：BE-INFRA-06/07（APScheduler reload_schedule、SPA 托管防穿越）、FE-ADMIN-TASK 整组 7 条（SchedulePage 零测试文件）、FE-ADMIN-LOGS 整组 3 条（LogCenterPage 零测试文件，其中 FE-LOG-04 批量删除虽标 P0 但其前端缺口随整页缺失）、FE-GLOBAL-03/05/06/08/09、FE-NAV-02、FE-SNAP-01/06/07 等。完整清单已在各子审计过程中核对，此处备查不展开。

---

## 四、误报记录（用于迭代体检提示词）

| 原编号 | 内容 | 误报原因 | 提示词迭代条款 |
| --- | --- | --- | --- |
| 原 F8 | 称 charts 四个 option 构建模块（557 行）"生产零引用、仅测试可达"，建议删除 | 主审复核证伪：四个组件均有显式 `import { buildXxxOption }` 生产导入 | 死代码判定前，必须对每个候选文件名执行"import 语句级正则"（含相对路径与 @别名两种形式）并回报命中计数；禁止以"逐一目视核对"代替正则证据 |
| 报告2· papaparse 结论 | 称 papaparse"有 1-2 处真实引用"应保留 | 2026-08-25 复核证伪：仅剩 vite.config.ts:46 manualChunks 分包字符串，零运行时 import，实为废弃依赖 | 依赖"在用"判定须区分「构建配置字符串引用」与「真实 import」，前者不构成保留依据 |

另：报告2 摘要表内部计数不自洽（安全维度标 1/1 但条目实际 2 高），其数据未纳入本终版汇总。

## 五、联动裁决项（人工标注时请一并处理）

1. REP-012 与 REP-005 第 7 条（BE-PF-08）：删端点 或 补测试，二者互斥。
2. REP-001 与验收表 BE-HLTH-07：方向一致（删除），仅需确认冒烟测试引用同步移除。
3. REP-029/030（删 overview 旧版图表）与 REP-031/032/033（charts 共享收敛）：前者是"删旧用新"，后者依赖前者完成后行数才足额兑现，建议同轮或先 29/30 后 31-33。
4. REP-004（httpx 升格）与阶段 3 第 1 轮"未使用依赖清理"：第 1 轮应同时包含"移除 3 个废弃依赖 + 升格 1 个错位依赖 + 替换 .env.example 占位符（REP-054）"，lockfile 变更一并审查。
5. REP-044/045（overview composables 收敛）建议与 REP-029/030 同视角处理：均为 overview 模块的"旧版副本"问题，可安排在同一轮次统一清理。
6. REP-051（api.ts 空壳导出）与 docs/openapi.json 过时问题绑定：先裁决 openapi.json 是重新生成还是停止维护，再决定 paths/operations 去留。
7. ~~附带观察（不计入总数）：web/src/composables/use-default-date-range.ts 与 web/src/modules/query/use-default-date-range.ts 并存且分别被不同页面引用，功能高度疑似重复；因本次未完成逐行相似度举证，列为 [待人工确认]。~~ **已裁决并收敛（2026-08-26）**：逐行比对确认两份实现同构（仅 `computed` 泛型标注与注释差异）；保留 modules/query 版为单一真相源（生产引用 4:2 占多数），删除 composables 版并改写 HoldingsPage.vue / analysis/use-range-preference-sync.ts 及其测试 mock 共 3 处引用；`vue-tsc --noEmit` 全量通过（vitest 因本环境已知损坏未跑，受影响测试仅改 mock 路径）。

## 六、汇总统计

| 口径 | 数量 |
| --- | --- |
| 总问题数（去重合并后） | **55 条** + 1 条附带观察 + 2 条误报记录 |
| 各维度分布 | 死代码 13、重复模块+冗余调用 16、废弃依赖/接口/残留 10、缺失测试 5 组、安全隐患 11（2026-08-26 复核更正：重复 15→16、废弃 11→10，总数 55 与风险分布不变） |
| 高/中/低分布 | 高 7、中 14、低 33、中低 1 |
| 预计可删除/减少代码行数 | **约 1060~1090 行**（六维版 664~686 + 补录项约 395~405：use-snapshots 36、use-query-data ≈45、死导出 ≈60、测试辅助去重 ≈250、_shares_at 4、转出口 2 等；REP-015 若确认另减 ~35 行） |
| 预计可移除依赖 | 3 个 npm 包（date-fns、papaparse、@types/papaparse）+ vite.config 2 条死分包分支 |
| 需迁移而非移除 | 1 个（httpx dev -> dependencies，已核实） |
| P0 测试缺口 | 后端 10 条 + 前端 4 条 + 部分 6 条 |

值得肯定的面（审计确认扎实，无需处理）：SQL 注入零发现（原生 SQL 参数绑定规范）、路径遍历零发现（uuid 重命名+魔数嗅探+双重闸门）、敏感信息零泄漏、组合级属主隔离到位、services/base.py 与 date_utils.py 是实打实的复用基类而非空壳抽象、"删除级联+重算"链路与主数据 skipped 规则测试非常完整。

## 七、下一步（进入阶段 3 的前置条件）

1. 人工对本报告逐条标注：采纳 / 搁置 / 误报（规则 R2.3：仅"采纳"项进入阶段 3 任务池）。
2. 处理第五章联动裁决项。
3. 采纳项整理为阶段 3 轮次任务池，建议顺序：
   - **第 1 轮 · 依赖瘦身**：REP-004/039/040/041 + REP-054（.env.example 占位符）
   - **第 2 轮 · 死文件死代码**：REP-020~028、REP-046~050（REP-051 待 openapi.json 裁决）
   - **第 3 轮 · 重复模块合并（一）**：REP-029/030 优先 → REP-031~033 → REP-044/045（overview composables 收敛）
   - **第 4 轮 · 重复模块合并（二）+ 冗余拍平**：REP-052（测试辅助提取）→ REP-034~037、REP-047
   - **第 5 轮 · 补 P0 测试**：REP-005~007（P1/P2 随穿插）
   - **缺陷修复池（非删除，单独排期立即处理）**：REP-001~003、008~011（安全类）+ REP-053（serialize_user 合并修复）
4. 误报原因已回流入第四章，下一轮体检提示词按迭代条款更新。

## 八、本终版的合并来源与验证记录

| 来源 | 采纳情况 |
| --- | --- |
| 《代码体检报告-2026-08-25》（六维版，43 条） | 全量采纳为骨架（REP-001~043） |
| 《代码体检报告》（第一份，14 条） | 独有条目已补录：REP-044/045（composables 重复分叉）、REP-046（_shares_at）、REP-047（悬空转出口）；其余与六维版重叠（RCE→REP-003、令牌时效→REP-011、schedule/守卫测试缺口→REP-005/007、骨架组件→REP-020~022、now_app_tz→REP-028） |
| 《代码体检报告2》（17 条） | 独有条目已补录：REP-048（13 死导出）、REP-049/050（过度导出/stringCodec）、REP-051（api.ts 空壳）、REP-052（测试辅助 21 复制）、REP-053（auth user dict）、REP-054（.env.example 密钥样例）、REP-055（游离文件）；其 papaparse"在用"结论经复核判误报剔除，其内部计数矛盾未采纳 |
| 交叉验证 | 以下结论经 2026-08-25 对当前工作树直接核验属实：REP-003（shell=True）、REP-004（httpx dev 组 + 运行时 import）、REP-040（papaparse 零 import）、REP-044/045（双文件存在）、REP-046（零命中）、REP-047（悬空转出口）、REP-048 抽样 4 符号（零引用）、REP-052（恰好 21 文件） |
| 全量复核（2026-08-26） | 对全部 55 条逐条回源码取证复核，约 90% 位置精确命中、关键计数全部吻合；据此修订 6 处：REP-045 四件套→五件套（useYearStartXirr 在 analysis 版 :93 同样存在）、REP-014 字段名更正为 QUOTE_SYNC_SCHEDULER_ENABLED/_CRON、REP-031 补第 4 份 AxisTooltipParam 副本、REP-053 标注 createdAt 缺陷疑已修复待确认、REP-051 行号更正（paths :5 / operations :1007）、第六章维度分布更正（重复 16 / 废弃 10）。详见 docs/archive/代码体检报告-终版-核对报告.md |
