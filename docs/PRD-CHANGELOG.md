# 投资收益统计系统 — PRD 修订史（Changelog）

> 本文件合并自 `docs/PRD.md` 顶部的「版本修订说明」块（读者向变更摘要）与 §13「覆盖结论」中的逐版 **ID 核算**说明（审计向）。主 PRD 仅保留版本行 + 指针，完整历史在此维护。
> 规则：每次 PRD 修订，本文件追加一条 `## vX.Y.Z` 条目（变更说明 + ID 核算）；无需求 ID 增减时（如结构性重构）也须记录。主 PRD 顶部仅保留一句「近期变更」。
> 📍 **索引同步规则（已废止）**：原要求 PRD 修订引起章节行号位移时**必须**同步刷新 `docs/PRD-INDEX.md`（快速导航表 + 章节速查表 + 附录索引三处）。该索引文件已于 2026-08-05 经 commit `23517b5` 删除（`PRD.md` / `ARCHITECTURE.md` 正文均不依赖该索引），故此硬性同步规则**自该日起废止**，无需再维护行号锚点；CHANGELOG 条目中可不再注明「索引未变」。

> 📌 **合计口径说明（承 §13 覆盖结论，逐字迁入）**：🔴 **遗漏 = 0，达成交付标准。** 三份来源共 **235 个**需求 / 约束 / 决策 / 风险 / 问题 / 规格条目全部在本 PRD 落位，另加 v3.1.0 新增派生 ID **2 个**、v3.1.1 新增派生 ID **2 个**，合计 **239**（实际枚举表格行 **221 行**，其中 7 行将多个用户故事 / 问题 ID 归并为一行，如 `US-D1~US-D5`、`Q-01~Q-14`）。含明确改判 / 废止并记录的 D-01、HOLD-P0-02/05/07、D-04、D-05、C-09、TXN 模块并入等。

## v3.1.8（2026-08-04）
**变更说明（注销账户自助恢复能力定义，新增 1 个需求 ID）**：① 精炼 `SET-P1-06` 验收标准（由 6 条扩为 8 条），明确"30 天冷静期 + 登录页冷静期信号 + 自助恢复"机制，并钉死"严禁联系客服"文案纪律；② **新增 `SYS-P1-02`**（账户注销冷静期与自助恢复），承载登录页冷静期信号 + `POST /api/auth/account/restore` 接口，全站唯一恢复通路，不设管理后台；③ 新增 §6.10.1 接口契约（状态机 / 入参出参 / 错误分支 / 登录冷静期信号四表）；④ 新增 §7.10 登录页冷静期恢复草图（状态 A/B 双草图 + 交互口径表）；⑤ 附录 E API 列表补 `POST /api/auth/account/restore` 与登录接口冷静期信号扩展。
**ID 核算**：📌 **v3.1.8 新增 `SYS-P1-02`（+1），合计 239 / 枚举行 221 → 240 / 222 / 遗漏 0**。稳定 ID 纪律：未删除、未复用任何既有 ID；`SET-P1-06` 验收扩展不新增 ID。`SYS-P1-02` 因恢复发生在未登录态登录页、无法落位设置页任一分区，故不入 `SET-` 命名空间，与 `SYS-P0-01` 同族。

## v3.1.7（2026-08-03）
**变更说明（锚点修复 + 编号补全，无需求 ID 增减）**：① 修复 2 处「见 §13」纯文本弱引用为直链 `./PRD-COVERAGE-MATRIX.md`（L164 已废止行、L704 ID 稳定性承诺行），消除抽取 §13 后需两跳才达真实内容的割裂；② 补全 §1.4 下两个子节编号 `### 1.4.1 In Scope` / `### 1.4.2 Out of Scope（v1 明确不做）`，为后续全文手动索引铺路。
**ID 核算**：合计仍为 **239 / 枚举行 221 / 遗漏 0**（本次纯结构修正，未触及任何需求 ID、未改 F0 冻结内容 §3 / `FIN-*` / 附录 A / 附录 B）。

## v3.1.6（2026-08-03）

**变更说明（结构性重构，无需求 ID 增减）**：修订史与 §13 新旧覆盖对照表自 `docs/PRD.md` 析出至独立文档 —— `docs/PRD-CHANGELOG.md`（本文件，合并版本修订说明 + 逐版 ID 核算）与 `docs/PRD-COVERAGE-MATRIX.md`（§13 全文）。主 PRD 顶部修订块与 §13 正文替换为指针；附录 E 标注「完整契约以 ARCHITECTURE §4.2 为权威」。阅读效率提升：主 PRD 移除约 352 行溯源/历史内容。

**ID 核算**：合计仍为 **239 / 枚举行 221 / 遗漏 0**（§13 覆盖结论表原样迁入 COVERAGE-MATRIX.md，计数不变）。本次为纯文档结构重构，未触及任何需求 ID、未改 F0 冻结内容（§3 金融口径 / `FIN-*` / 附录 A / 附录 B）。

## v3.1.5（2026-08-03）

**变更说明**：**v3.1.5 修订（口径矛盾修复 + 回归门禁补齐，无需求变更、无 ID 增减）**：**P1 —— 修 `C-09′` 自相矛盾**：§2.4 改判表 `C-09` 行原文只列「证券买卖 / 现价更新」2 类却自称「五类触发事件之一」，现改为**直接指向 §2.5 `C-09′`**（五类全列 + 两层分流，不再重列以杜绝二次漂移）；§2.5 `C-09′` 行补**两层分流**——**T1~T4**（出入金/买卖/现价/现金余额）触发**快照层区间重建 + 计算层级联 `[D, today]`**，**T5**（手工总资产记录增改删重置）触发**快照层仅当日（不重建自动记录）+ 计算层级联 `[date, today]`**，严禁「只写快照不重算净值」。**P2 —— 新增 `REG-06` P0 强制门禁**：镜像 `docs/ARCHITECTURE.md` v2.1 §13，收编「手工修改历史日期 → 其后净值/XIRR 已级联更新」为第六条强制回归测试（§10.3 清单行 + 明细小节 4 步 / A~D 断言 / 代码级断言），门禁范围由 `REG-01`~`REG-05` 五项扩为 **`REG-01`~`REG-06` 六项**（目录表、§10 章首、§10.1 通过标准、§10.4 对应关系表、W-5 处置列同步）。**`REG-*` 为回归测试标识，非需求 ID**，§13 合计仍为 **239 / 枚举行 221 / 遗漏 0**。**F0 冻结内容（§3 金融口径、`FIN-*`、XIRR / 净值 / 份额公式、精度、附录 A、附录 B）一字未改** —— 本次仅涉级联行为与回归断言，不触碰任何计算公式。

**ID 核算**：📌 **v3.1.5 修订说明（`C-09′` 口径矛盾修复 + `REG-06` 回归门禁补齐）**：**无需求变更、无 ID 增减**，合计仍为 **239 / 枚举行 221 / 遗漏 0**（`REG-*` 是**回归测试标识**，不占用 `§6` 需求 ID 空间，故新增 `REG-06` **不触发 §13 覆盖表任何计数变化**）。本次两块改动：**【P1 · 修矛盾】** §2.4 架构冲突裁决表 `C-09` 行的「新口径」列原文只枚举「证券买卖 / 现价更新」**2 类**，却自称「**五类触发事件之一**」——**自相矛盾**，且与 §2.5 `C-09′` 的五类枚举不一致；现改为**五类全列 + 两层分流 + 直接指向 §2.5 `C-09′` 与 §8.4**，并明示「本行不再重列」以杜绝二次漂移。同时 §2.5 `C-09′` 行补齐**两层分流**口径：**T1~T4**（出入金 / 证券买卖 / 现价更新 / 现金余额变更）→ **快照层区间重建 + 计算层级联 `[D, today]`**；**T5**（手工总资产记录增/改/删/重置）→ **快照层仅当日（不重建自动记录）+ 计算层级联 `[date, today]`**，并写入「**严禁「只写快照不重算净值」**」硬约束，对齐 `docs/ARCHITECTURE.md` v2.1 §7.3.1 / `C-13`。§8.4 计算触发规则表原文已正确（T1~T4「级联重算 + 区间重建自动记录」vs T5「从该日起重算 NAV/XIRR（不重建自动记录）」），**保留未动**，作为 `C-09′` 的落地口径被引用。**【P2 · 补门禁】** 新增 **`REG-06`「手工修改历史日期 → 其后净值 / XIRR 已级联更新」**，镜像自 `docs/ARCHITECTURE.md` v2.1 §13（架构侧已落地，PRD 侧收编为产品级门禁）：§10.3 清单表新增 `REG-06` 行 + 必要性说明，并新增 **`### REG-06` 明细小节**（4 步流程 + A~D 四条断言 SQL + 三条代码级附加断言 + `INV-1`）。补齐动因：原 `REG-01`~`REG-05` **五条全部只覆盖快照层**（每日唯一 / 手工不被覆盖 / 可重置），**无一条校验计算层级联**，而 §2.4 早已把「`snapshot.upsert()` 覆盖历史快照只重算当日、未做级联重算」列为**必修项**却无门禁 —— 五条全绿仍可能交付「改了历史总资产、其后净值/XIRR 纹丝不动」的**静默数据错误**。门禁范围同步由「五项」扩为「**六项**」，共 5 处：目录表 §10 行、§10 章首警示、§10.1 通过标准、§10.4 对应关系表（新增 `REG-06` 行 + 「全部」行改 `REG-01 ~ REG-06`）、§11 风险登记册 W-5 处置列。§10 来源说明补注「**`REG-06` 为 v3.1.5 新增，非 v2.3 原文**」，保持「原文五条逐条原样搬入」的可追溯性不被污染。**F0 冻结内容（§3 金融口径、`FIN-*`、XIRR / 净值 / 份额公式、精度约定、附录 A、附录 B）一字未改** —— 本次全部改动均属**级联行为口径与回归断言**层面，**未触碰任何金融计算公式**。

## v3.1.4（2026-08-03）

**变更说明**：**v3.1.4 修订（配色口径统一，纯展示层，无需求变更、无 ID 增减）**：**全局涨跌配色统一为 A 股惯例「正值 = 红色、负值 = 绿色」**。① 修正 `ANL-P0-06` 每日收益配色（原「正收益绿色、负收益红色」→「正收益红色、负收益绿色」）；② 修正 §7.6 净值分析页线框图注释（原「正收益绿色 / 负收益红色」→「正收益红色 / 负收益绿色」）；③ 新增 **§9.5 展示规范 —— 全局涨跌配色约定**（无 ID 规范章节，体例同 §9.1~§9.4），并在 §2.5 全局约束表后补交叉引用。裁决来源：**用户（产品负责人）拍板「PRD 全部改成 A 股正红负绿」**。`FLOW-P0-07` / `DASH-P0-05` / `DASH-P1-05` / `HOLD-B-P0-04` 原文即为正红负绿，**未作改动**。**F0 冻结内容（§3 金融口径、`FIN-*`、附录 A、附录 B、`rate ≤ -0.999 → -0.999` 钳制）一字未改**，§13 合计仍为 **239 / 枚举行 221 / 遗漏 0**。

**ID 核算**：📌 **v3.1.4 修订说明（配色口径统一 · 纯展示层修订）**：**无需求变更、无 ID 增减**，合计仍为 **239 / 枚举行 221 / 遗漏 0**。本次仅统一「涨跌 / 盈亏 / 金额增减」的正负语义配色，全局采用 **A 股惯例：正值 = 红色、负值 = 绿色**，消除 PRD 内部反向表述。具体改动三处：① **`ANL-P0-06`**（§6.7 分析页）验收 1 由「正收益**绿色**、负收益**红色**」修正为「**正收益红色、负收益绿色**（A 股涨跌色，遵循 §9.5）」；② **§7.6 净值分析页线框图注释**（每日净值明细表下方）由「正收益绿色 / 负收益红色」修正为「**正收益红色 / 负收益绿色**」；③ **新增 §9.5「展示规范 —— 全局涨跌配色约定（A 股惯例 · 正红负绿）」**，明确适用范围（图表 / 明细表 / 卡片 / 徽标）、色值必须走主题变量、与正负语义无关的着色不受约束、**后续新增需求不得出现反向配色**，并在 §2.5 全局约束表后补一条交叉引用。**§9.5 为无 ID 规范章节**（体例与 §9.1 精度 / §9.2 性能 / §9.3 安全性 / §9.4 多端一致性 一致），**不新增需求 ID，不触发 §13 覆盖表任何计数变化**。原文即符合正红负绿的 `FLOW-P0-07`、`DASH-P0-05`、`DASH-P1-05`、`HOLD-B-P0-04` **一字未改**。**裁决来源：用户（产品负责人）拍板 ——「PRD 全部改成 A 股正红负绿」**，架构评审阶段由架构师标注的「ANL-P0-06 配色矛盾，待 PM 澄清」至此闭环。**F0 冻结内容（§3 金融口径、`FIN-*`、XIRR / 净值 / 份额 / 精度、附录 A、附录 B）一字未改**，本次为纯展示层修订。

## v3.1.3（2026-08-03）

**变更说明**：**v3.1.3 修订（D-1 钳制修正，F0 冻结内容授权例外）**：XIRR 溢出钳制阈值由 `rate ≤ -1` 同步为 `rate ≤ -0.999`（钳定值 −0.999 不变）。依据：现网代码 `packages/finance-core/src/xirr.ts` 的 `MIN_RATE = -0.999` 为更宽、更安全的下钳边界（多钳住 [−100%, −99.9%] 区间），用户明确授权违反“代码逐行对应 PRD”的 F0 规则，将 PRD 反向对齐至代码。属 F0 冻结文本的唯一授权例外，其余 F0 内容（§3 / `FIN-*` / 附录 A 其余行 / 附录 B）一字未改。

**ID 核算**：📌 **v3.1.3 修订说明（D-1 钳制修正 · F0 冻结内容的唯一授权例外）**：**无需求变更、无 ID 增减**，合计仍为 **239 / 枚举行 221 / 遗漏 0**。本次仅同步 XIRR 溢出钳制的**触发阈值**：`rate ≤ -1` → **`rate ≤ -0.999`**，**钳定值 −0.999 保持不变**（钳完仍为 `-0.999`，两者差别仅在触发边界）。依据：现网代码 `packages/finance-core/src/xirr.ts` 中 `const MIN_RATE = -0.999;` 且 `if (rate <= MIN_RATE)`，即代码采用**更宽、更安全**的下钳边界——比 PRD 原 `≤ -1` 多钳住 **[−100%, −99.9%]** 区间，避免该段极端负收益率裸放进 `(1+r)^t` 幂运算。经**用户明确授权**，本次**例外性地违反 `FIN-F0-05`「实现必须逐行对应 PRD」规则**，改为**将 PRD 反向对齐至代码**。此为 F0 冻结文本的**唯一授权例外**：Newton-Raphson 其余参数（r₀=0.1 / maxIter=100 / tolerance=1e-7 / `dnpv == 0` 中断 / 同日合并 / 全同号返回 `null`）、§3 金融口径、其余 `FIN-*`、附录 A 除该行外的全部行、附录 B（`calculateDailyNav` 与期末口径注释）**一字未改**。

## v3.1.2（2026-08-03）

**变更说明**：**v3.1.2 修订（纯修缮，无需求变更）**：**补登遗漏的 `SET-P0-07` 枚举行** —— `SET-P0-07`（软提示开关，P0）需求本体自 v3.0 起已存在（§6.9 L863、`UserPreference.cashHintOnCashflow/cashHintOnTrade` L890-891、§6.9.2 偏好与外观区 L902），且已计入 §13 合计 239 与覆盖结论；唯独**漏登于 §13 设置模块枚举表**（L1751 与 L1752 之间）。本次仅补该展示行，**不新增 ID、不改动任何口径、不触发优先级变化**，合计仍为 **239 / 遗漏 0**，枚举行由 220 → 221。**F0 冻结内容（§3 金融口径、`FIN-*`、附录 A、附录 B、`rate ≤ -0.999 → -0.999` 钳制）一字未改。**

**ID 核算**：📌 **v3.1.2 修订说明（相对 v3.1.1 的 239 项）**：**纯枚举补登，无 ID 增减**。`SET-P0-07`（软提示开关，P0）需求本体自 v3.0 起已存在（§6.9、§6.9.1 `UserPreference` 字段、§6.9.2 偏好与外观区），亦已计入 §13 合计 239 与覆盖结论，唯独**漏登于 §13 设置模块枚举表**，本次补该展示行。故 **ID 数仍为 239（无增减）**，**枚举行由 220 → 221**（补登 1 行）。合计 239 / 遗漏 0 不变，F0 冻结内容一字未改。

## v3.1.1（2026-08-03）

**变更说明**：**v3.1.1 修订**（含需求变更，非纯修缮）：**账户生命周期与会话操作全部迁入设置页** —— `ACC-P1-03`（注销账户）与「退出登录」自账户页 `/account` **迁入设置页** `/settings`，账户页自此**零修改、零登出**，仅保留只读展示 + 跳转入口；依 §6.0 ID 稳定性承诺采用「**新增 ID + 旧 ID 保留为迁移记录**」，派生 **`SET-P0-09`**（退出登录，P0）与 **`SET-P1-06`**（注销账户，P1）两个新稳定 ID，旧 `ACC-P1-03` 行保留并标注「已迁入 `SET-P1-06`」；涉 §5.7、§5.8、US-A4、`ACC-P0-05`（「会话与跳转区」→「**跳转区**」）、`ACC-P1-03`、`SET-P0-09`、`SET-P1-06`、§13 覆盖表同步（237→239，遗漏仍为 0）。**F0 冻结内容（§3 金融口径、`FIN-*`、附录 A、附录 B、`rate ≤ -0.999 → -0.999` 钳制）一字未改。**

**ID 核算**：📌 **v3.1.1 修订说明（相对 v3.1.0 的 237 项）**：因**退出登录**（原属 `ACC-P0-05` 会话动作）与**注销账户**（原 `ACC-P1-03`）**自账户页迁入设置页**，依 §6.0 **ID 稳定性承诺（只增不改、旧 ID 不复用、不删除）**，采用「**新增 ID + 旧 ID 保留为迁移记录**」处理，派生 **`SET-P0-09`**（退出登录，P0）/ **`SET-P1-06`**（注销账户，P1）两个新稳定 ID；旧 `ACC-P1-03` 行**保留不删除**，仅改写为迁移记录并把「新 PRD 落位」指向 `SET-P1-06`；`ACC-P0-05` 保留原 ID 并改名「**跳转区**」（功能收敛为纯跳转）。故 ID 数由 237 → **239**，枚举行由 218 → **220**。**源 PRD 三份的 235 项覆盖结论不变，遗漏数仍为 0。**

## v3.1.0（2026-08-03）

**变更说明**：**v3.1.0 修订**（含需求变更，非纯修缮）：① **附录 B 净值伪代码恢复为 `PRD.md` 附录 B 逐字原文**（补回被压缩合并的赋值拆分与 3 条注释，含「期末口径：assetSnapshot 含当日进出，需还原为申赎前资产再计算净值」），与附录 A 同达"逐字冻结"标准，**语义等价、金融口径零变化**；② **账户 / 设置模块职责重划**——账户页 `/account` 收敛为**纯只读**聚合视图，邮箱 / 密码 / 资料 / **头像**四类修改动作**全部收归设置页** `/settings` 账户区（`SET-P0-01` 为全站唯一入口），消除双页重复（涉 §5.7、§5.8、US-A4、`ACC-P0-02`、`ACC-P0-05`、`SET-P0-01`）；③ **两条需求升级 P0**——`ACC-P1-01`→ 新增 `ACC-P0-06`（数据统计卡）、`SET-P1-01`→ 新增 `SET-P0-08`（外观主题），依 §6.0 ID 稳定性承诺采用「新增 ID + 旧 ID 保留为升级记录」，联动 `GET /api/account/stats` 升 P0、`UserPreference.theme` 标注升 P0、§13 覆盖表同步（235→237，遗漏仍为 0）。**F0 冻结内容（§3、`FIN-*`、附录 A、`rate ≤ -0.999 → -0.999` 钳制）一字未改。**

**ID 核算**：📌 **v3.1.0 修订说明（相对 v3.0.1 的 235 项）**：因 `ACC-P1-01`（数据统计卡）与 `SET-P1-01`（外观主题）升级为 P0，依 §6.0 **ID 稳定性承诺（只增不改、旧 ID 不复用）**，采用「**新增 ID + 旧 ID 保留为升级记录**」处理，派生 **`ACC-P0-06`** / **`SET-P0-08`** 两个新稳定 ID；旧 `ACC-P1-01` / `SET-P1-01` 行**保留不删除**，仅改写为升级记录并把「新 PRD 落位」指向新 ID。故 ID 数由 235 → 237，枚举行由 216 → 218。**源 PRD 三份的 235 项覆盖结论不变，遗漏数仍为 0。**

## v3.0.1（2026-08-03）

**变更说明**：**v3.0.1 修订**：① 新增 **§10 验收与强制回归测试**（`REG-01`~`REG-05`，P0 强制门禁，原 v2.3 §9.3）；② §3.7 补「完整参数以附录 A 为权威」指引；③ §9.4.1 补入双端交互差异表（原 `PRD.md` §6.3），消除悬空引用。原 §10/§11/§12 顺延为 §11/§12/§13。**金融口径（§3、FIN-D5、附录 A/B）一字未改。**

**ID 核算**：📌 **v3.0.1 修订说明（相对 v3.0 首版 232 项）**：本次新增 3 个章节级条目的显式核对行 —— ① `PRD.md §5.3` XIRR 迭代参数（原仅隐含于 §3.7 / FIN-D5 / 附录 A，本次显式登记）；② `PRD.md §6.3` 双端交互差异表（**首版遗漏，本次补入 §9.4.1**）；③ `v2.3 §9.3` 强制回归测试 5 项（**首版被打散，本次提升为独立章节 §10 `REG-01`~`REG-05`**）。故 ID 数由 232 → 235，枚举行由 213 → 216。**首版 232 项的覆盖结论不变，遗漏数仍为 0。**

---

> 主规格见 **[docs/PRD.md](./PRD.md)** ｜ 覆盖对照见 **[docs/PRD-COVERAGE-MATRIX.md](./PRD-COVERAGE-MATRIX.md)**
