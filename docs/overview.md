# 概览 · investment_return_tracker 代码审查（2026-08-09）

## 做了什么
对 FastAPI 重写的投资回报追踪系统做需求覆盖率 + 实现缺陷审计（只读，未改任何文件）。团队：架构师（需求覆盖率）、QA（缺陷核查）。

## 关键结论
- **后端**：55 端点对应 PRD **P0 主线全绿**；冻结口径/不变量（FG/G/C、信封、Decimal→str、JWT、口径分离、每日唯一总资产）**100% 满足**。
- **覆盖率**：129 条需求中 ✅≈86% ｜ 🟡≈11% ｜ ⚠️≈2%（含 1 误报）｜ ❌≈1%（均为 PRD 标注的 P2 增强）。
- **QA 缺陷**：🔴 H1 XIRR 极端值 NUMERIC(20,8) 溢出致重算事务 500（立即修）；🟠 M1 首笔存入校验缺失 / M2 多 securityId 静默丢数据 / M3 收益率量纲百分比 vs 比值 / M4 导入数量价格限 2 位；🟡 L1–L5 边界；⚠️ N1 持仓成本口径 Python(含费) vs NestJS(含费单价) 需拍板。
- **6 处代码-需求偏差**：D1 误报（XIRR 矛盾不存在）｜ D2 recalculatedDays 未回传 ｜ D3 quarter 粒度不支持 ｜ D4 SecurityType.CASH 未弃用 ｜ D5 account_stats 缺计数 ｜ D6 P1/P2 缺口。
- **纠正两处过时记忆**：① 前端 `web/src` 实际已完整实现（非"待开工"）；② "XIRR Newton 矛盾"为误报，新项目 ARCHITECTURE.md §7.1 已写明 pyxirr。

## 交付物
- 完整报告：`docs/code-review-2026-08-09.md`
- 修复优先级：H1/M1/M2 立即修；M3/M4 尽快；L1–L5 排期；N1 需决策。

## 下一步建议
1. 先修 H1（量程保护 + 单日 try/except）防重算崩溃。
2. 修 M1/M2（PRD 硬约束 + 静默丢数据）。
3. 统一收益率量纲（M3）与导入精度（M4）。
4. 决策 N1 跨后端口径对齐。
