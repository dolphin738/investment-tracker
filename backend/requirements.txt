# 后端依赖锁定清单（与 backend/pyproject.toml 同步）
# 由 dev-env.ps1 用于 pip install -r requirements.txt
# 升级版本时请同步修改 pyproject.toml

# ── 主依赖（生产）──
fastapi==0.141.1
uvicorn[standard]==0.52.1
pydantic==2.13.4
pydantic-settings==2.15.0
sqlalchemy==2.0.51
greenlet==3.1.1
asyncpg==0.31.0
alembic==1.19.0
PyJWT==2.13.0
bcrypt==5.0.0
python-multipart==0.0.32
pyxirr==0.10.8

# ── 开发 / 测试依赖 ──
pytest==9.1.1
pytest-asyncio==1.4.0
httpx==0.28.1
# starlette 1.4 testclient 首选 httpx2（httpx 官方延续版，pydantic 团队维护）；装上即消除 StarletteDeprecationWarning
httpx2==2.9.1
