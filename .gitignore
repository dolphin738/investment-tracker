# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Env / secrets (real credentials — never commit)
backend/.env
.env
.env.*
*.env
*.env.*
*.local
# keep example templates (no real secrets committed)
!*.env.example
!.env.example

# Keys / credentials / cloud config
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.pub
*.ppk
*.tfvars
*.tfstate
*.tfstate.backup
.netrc
npmrc
service-account*.json
firebase*.json
gcloud*
*.db
*.sqlite
*.sqlite3

# Uploads (user data)
backend/uploads/
web/dist/
web/node_modules/

# Frontend build / cache / logs
web/.turbo/
web/.qa-cache/
web/dist-node/
web/vite.config.ts.timestamp-*.mjs
*.tsbuildinfo
*.log

# OS
.DS_Store
Thumbs.db

# Editors
.idea/
.vscode/

# Local agent memory (not for repo)
.workbuddy/

# Local generated docs (e.g. ls snapshots)
docs/ls/

# Project helper scripts (local dev utilities, not part of repo deliverable)
scripts/
